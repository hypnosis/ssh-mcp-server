# Findings the guard may skip. Format follows .gitignore.
# A line stores the digest of a finding, never the value behind it.

# README, the scp example in the section about legacy routers and NAS boxes:
# the address there is made up and points at no machine of ours.
ssh-target:29b71b2af018

# scripts/lab-up.sh, the home directory of the unprivileged user it creates:
# the machine is a throwaway lab container built by that script, not a host of ours.
home-path:70610edc4d73

# scripts/lab-up.sh and the live sudo test, the home directory of the user that
# logs in by key and answers sudo with a password: the same throwaway container.
home-path:30e238709813
