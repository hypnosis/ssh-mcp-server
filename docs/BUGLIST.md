# 🐛 БАГЛИСТ SSH MCP Server

**Дата создания:** 2025-01-11
**Статус:** ✅ Тестирование завершено

---

## 📋 Найденные баги

### ❌ Критичные баги

*Нет открытых критичных багов* ✅

---

### ⚠️ Известные проблемы (не критичные)

#### ✅ ISSUE-001: Тильда не раскрывается в путях файлов [ИСПРАВЛЕНО]
**Описание:** При использовании `ssh_file_read` с путями содержащими тильду (`~`), команда `cat` не может найти файл
**Статус:** ✅ ИСПРАВЛЕНО в v1.1.0 (Sprint 3 - 2025-01-15)
**Приоритет:** СРЕДНИЙ
**Воспроизведение (до исправления):**
```
ssh_file_read("~/.bashrc") → Error: cat: '~/.bashrc': No such file or directory
```
**Решение:**
Реализовано автоматическое раскрытие тильды через `$HOME`:
- `~/file` → `$HOME/file` → `cat "$HOME/file"` → shell раскрывает `$HOME` ✅
- `~user/file` → `~user/file` → shell раскрывает `~user` ✅
- Используются двойные кавычки для раскрытия `$HOME`
- Все остальные спецсимволы экранируются для безопасности

**Компоненты:** 
- `src/tools/file-tools.ts` - добавлены методы `expandRemoteTilde()`, `buildSafeCommand()`
- `src/tools/log-tools.ts` - добавлены методы `expandRemoteTilde()`, `buildSafePath()`

**Тестирование:**
- ✅ `ssh_file_read("~/.bashrc")` - работает
- ✅ `ssh_file_write("~/test.txt", "content")` - работает
- ✅ `ssh_log_tail("~/logs/app.log")` - работает
- ✅ Unit тесты добавлены в `tests/unit/path-security.test.ts`

**Дата исправления:** 2025-01-15
**Sprint:** Sprint 3 - Path Security & Tilde Expansion

---

## 🔴 АРХИТЕКТУРНЫЕ ПРОБЛЕМЫ (ОБНАРУЖЕНО 2025-01-12)

### 🗑️ ARCH-001: Отсутствие пулинга SSH соединений [СНЯТО ВМЕСТЕ С МЕХАНИЗМОМ]
**Описание:** Каждая команда создаёт новое SSH соединение (Connect → Auth → Exec → Close)
**Влияние:** 
- 10 команд подряд = 10 полных циклов подключения = 15-20 секунд
- Должно быть: 1 соединение для всех команд = 2-3 секунды
**Производительность:** **6-10× медленнее** чем могло бы быть
**Компоненты:** ~~`ssh-manager.ts`, `ssh-executor.ts`~~
**Статус:** 🗑️ СНЯТО — механизм, которого касался баг, удалён целиком

> **Поправка от 2026-08-17.** Пул на `ssh2` (`ssh-manager.ts`) удалён вместе с самой
> библиотекой `ssh2` — транспорт один, системный `ssh` (`CHANGELOG.md:10-13`). Проблема
> «каждая команда = новое соединение» решена другим механизмом: ControlMaster-мультиплексирование
> системного SSH, при котором все команды разделяют один мастер-сокет
> (`src/runner/ssh-args.ts:180-184`). Баг не «исправлен по плану Sprint 2» — план и код,
> которых он касался, больше не существуют.

### 🗑️ ARCH-002: executeBatch неэффективен [СНЯТО ВМЕСТЕ С МЕХАНИЗМОМ]
**Описание:** Batch команды выполняются последовательно, каждая с новым соединением
**Влияние:** 10 команд в batch = 10 соединений вместо 1
**Производительность:** **10× медленнее** для batch операций
**Компоненты:** ~~`ssh-manager.ts:99-112`~~
**Статус:** 🗑️ СНЯТО — `ssh-manager.ts` и пул на `ssh2` удалены

> **Поправка от 2026-08-17.** `ssh-manager.ts` в проекте больше нет. Batch-команды
> (`src/tools/exec-tool.ts:213`) по-прежнему идут циклом по одной, но каждая переиспользует
> общий ControlMaster-сокет вместо нового TCP+SSH-рукопожатия — не через выделенный
> `executeBatch` с пулом, как планировалось, а через транспорт, который делает то же самое
> для всех вызовов сразу (`src/runner/ssh-args.ts:180-184`).

### ✅ ARCH-003: Timeout race condition [ИСПРАВЛЕНО]
**Описание:** Timeout handler может вызвать reject() после того как Promise уже resolved
**Влияние:** Редкие ошибки, двойной вызов resolve/reject
**Компоненты:** ~~`ssh-manager.ts:86-89`~~ → `src/runner/process.ts`
**Статус:** ✅ ИСПРАВЛЕНО — в текущем транспорте

> **Поправка от 2026-08-17.** `ssh-manager.ts:86-89`, где был описан баг, удалён вместе с
> пулом на `ssh2`. В текущем транспорте (`src/runner/process.ts:123`) race condition закрыт
> флагом `settled`, который проверяется перед каждым resolve/reject
> (`src/runner/process.ts:156, 171-172, 179, 217`).

### ✅ ARCH-004: Неполное экранирование путей [ИСПРАВЛЕНО]
**Описание:** escapePath экранировал только single quotes, не все спецсимволы
**Влияние:** Потенциальные проблемы с путями содержащими спецсимволы
**Компоненты:** `file-tools.ts`, `log-tools.ts`
**Статус:** ✅ ИСПРАВЛЕНО в v1.1.0 (Sprint 3 - 2025-01-15)
**Решение:**
- Добавлены два метода экранирования:
  - `escapeForSingleQuotes()` - для обычных путей (безопаснее)
  - `escapeForDoubleQuotes()` - для путей с `$HOME` (экранирует `\`, `"`, `$`, `` ` ``, `!`)
- Стратегия: single quotes для обычных путей, double quotes только для `$HOME`
- Защита от injection атак (command substitution, variable expansion)
**Дата исправления:** 2025-01-15
**Sprint:** Sprint 3 - Path Security & Tilde Expansion

### ✅ ARCH-005: Нет валидации путей [ИСПРАВЛЕНО]
**Описание:** Отсутствовала whitelist/blacklist для путей, можно было читать любые файлы
**Влияние:** AI мог случайно читать чувствительные файлы (например `/etc/shadow`)
**Безопасность:** Не было ограничений на path traversal (`../`)
**Компоненты:** `file-tools.ts`, `log-tools.ts`, `path-validator.ts` (новый)
**Статус:** ✅ ИСПРАВЛЕНО в v1.1.0 (Sprint 3 - 2025-01-15)
**Решение:**
- Создан `PathValidator` с опциональной конфигурацией в профилях
- Поддержка `allowedPaths` (whitelist), `deniedPaths` (blacklist)
- Защита от path traversal (`allowTraversal: false`)
- Ограничение длины пути (`maxPathLength`)
- Интегрирован в `file-tools.ts` и `log-tools.ts`
**Конфигурация:** Опциональная, добавляется в SSH профили через `pathSecurity`
**Дата исправления:** 2025-01-15
**Sprint:** Sprint 3 - Path Security & Tilde Expansion

> **Поправка от 2026-08-04 (CORE_10, пункт 3.6).** Отметка «исправлено» была неверной:
> код и тесты существовали, а поле `pathSecurity` не доезжало от файла профилей до
> инструмента — валидатор не создавался ни разу, ограничений не было вовсе. Вскрылось
> живым замером на контейнере. Теперь поле проходит весь путь, правила действуют и в
> инструментах передачи, а проверка идёт **после** раскрытия `~`.

### ✅ ARCH-006: Profiles синглтон без reload [ИСПРАВЛЕНО]
**Описание:** Профили загружаются один раз при старте, нельзя перезагрузить без рестарта
**Влияние:** Неудобство при разработке и изменении конфигурации
**Компоненты:** `src/utils/profile-resolver.ts`
**Статус:** ✅ ИСПРАВЛЕНО

> **Поправка от 2026-08-17.** Реализовано так, как и планировалось: кэш с TTL
> (`SSH_MCP_PROFILES_CACHE_TTL`), file watcher на файл профилей и ручная перезагрузка через
> `reloadProfiles()` (`src/utils/profile-resolver.ts:1-8, 60-71, 176`).

---

## 📋 ПЛАН ИСПРАВЛЕНИЯ (ROADMAP)

> **Поправка от 2026-08-17.** План ниже — исторический (январь 2025), см. текущий
> `docs/sprints/ROADMAP.md`. Все пункты, ради которых он был написан, закрыты выше:
> Connection Pool снят вместе с `ssh2` (ARCH-001, ARCH-002), Path Security исправлен
> (ARCH-004, ARCH-005), Timeout — исправлен (ARCH-003), Profiles Reload — исправлен
> (ARCH-006).

См. детальный roadmap: `docs/sprints/ROADMAP.md`

**Спринты:**
1. 🔴 Sprint 2 (Jan 13-14): Connection Pool & Performance - **КРИТИЧНО**
2. 🟡 Sprint 3 (Jan 15): Path Security & Tilde Expansion
3. 🟡 Sprint 4 (Jan 16): Timeout & Error Handling
4. 🟢 Sprint 5 (Jan 17): Profiles Reload & Monitoring

**Ожидаемые улучшения:**
- ⚡ Производительность: **6-10× быстрее** (Connection Pool)
- 🛡️ Безопасность: Валидация путей, безопасное экранирование
- 🔧 Стабильность: Retry, auto-reconnect, timeout fix
- 📊 Удобство: Мониторинг, profile reload, debug tools

---

## ✅ Исправленные баги

### BUG-001: SSH Authentication Failed ✅ ИСПРАВЛЕН
**Описание:** Все SSH команды возвращали ошибку "All configured authentication methods failed"
**Статус:** ✅ ИСПРАВЛЕН
**Дата исправления:** 2025-01-11
**Приоритет:** КРИТИЧНЫЙ
**Решение:**
1. Проблема была в неправильном пути к SSH ключу в профиле
2. Исправлен путь в профиле конфигурации (неправильный ключ → правильный)
3. Добавлена явная проверка существования ключа с понятными ошибками
4. Удалён fallback механизм - нет магии, только явные пути

**Тестирование:**
- ✅ `ssh_exec` работает: успешное выполнение команды
- ✅ SSH подключение через обычный SSH работает
- ✅ MCP инструмент `ssh_exec` работает корректно

**Изменения в коде:**
- Добавлено детальное логирование в `ssh-executor.ts` (метод `connect`)
- Явная проверка `existsSync` для SSH ключа с чёткой ошибкой
- Удалён временный fallback механизм
- Ошибки загрузки ключа теперь пробрасываются явно (throw error)
- Логи показывают полную информацию о загрузке ключа, путях и параметрах подключения

---

## 📝 Заметки по тестированию

### Протестированные функции:
- [x] ssh_exec (одиночная команда) ✅
- [x] ssh_exec (батч команд) ✅
- [x] ssh_exec (с sudo) ✅
- [x] ssh_exec (с cwd) ✅
- [x] ssh_exec (опасные команды - предупреждение) ✅
- [x] ssh_file_read (одиночный файл) ✅
- [x] ssh_file_read (несколько файлов) ✅
- [x] ssh_file_read (с sudo) ✅
- [x] ssh_file_write (одиночный файл) ✅
- [x] ssh_file_write (несколько файлов) ✅
- [x] ssh_file_list (обычный список) ✅
- [x] ssh_file_list (с паттерном) ✅
- [x] ssh_file_list (рекурсивный) ✅
- [x] ssh_log_tail (одиночный лог) ✅
- [x] ssh_log_tail (несколько логов) ✅
- [x] ssh_log_search (простой поиск) ✅
- [x] ssh_log_search (с контекстом) ✅
- [x] ssh_log_search (несколько логов) ✅
- [x] ssh_snapshot (полный снимок) ✅

### Результаты тестирования:
- **ssh_exec**: ✅ РАБОТАЕТ (19 тестов пройдено)
  - Одиночные команды ✅
  - Батч команды ✅
  - Команды с sudo ✅
  - Команды с cwd ✅
  - Предупреждения о опасных командах ✅
- **ssh_file_read**: ✅ РАБОТАЕТ (3 теста пройдено)
  - Чтение одного файла ✅
  - Чтение нескольких файлов ✅
  - Чтение с sudo ✅
  - ⚠️ Замечание: тильда (~) не раскрывается, нужно использовать абсолютные пути
- **ssh_file_write**: ✅ РАБОТАЕТ (2 теста пройдено)
  - Запись одного файла ✅
  - Запись нескольких файлов ✅
- **ssh_file_list**: ✅ РАБОТАЕТ (3 теста пройдено)
  - Обычный список ✅
  - Список с паттерном ✅
  - Рекурсивный список ✅
- **ssh_log_tail**: ✅ РАБОТАЕТ (2 теста пройдено)
  - Последние строки одного лога ✅
  - Последние строки нескольких логов ✅
- **ssh_log_search**: ✅ РАБОТАЕТ (3 теста пройдено)
  - Простой поиск ✅
  - Поиск с контекстом ✅
  - Поиск в нескольких логах ✅
- **ssh_snapshot**: ✅ РАБОТАЕТ (1 тест пройден)
  - Полный системный снимок ✅

**ИТОГО: ВСЕ 19 ТЕСТОВ ПРОЙДЕНЫ УСПЕШНО** 🎉

---

## 🔍 Логи и дебаг информация

### ✅ Добавлено детальное логирование:

#### 1. `ssh-executor.ts`:
- ✅ Логирование начала подключения (host, port, username)
- ✅ Логирование загрузки SSH ключа (путь, проверка существования файла, валидация)
- ✅ Логирование метода аутентификации (privatekey/password)
- ✅ Логирование всех SSH событий (ready, error, keyboard-interactive)
- ✅ Детальное логирование ошибок подключения (код, уровень, полная информация)
- ✅ Логирование выполнения команд (команда, stdout/stderr размеры, exit code)

#### 2. `profile-resolver.ts`:
- ✅ Логирование разрешения профилей (запрошенный, доступные, default)
- ✅ Логирование деталей профиля (host, port, username, key path)
- ✅ Логирование расширения путей (~ → полный путь)
- ✅ Логирование валидации профилей
- ✅ Логирование финальной SSH конфигурации (без паролей)

#### 3. `profiles-file.ts`:
- ✅ Логирование загрузки файла профилей (путь, размер)
- ✅ Логирование валидации каждого профиля
- ✅ Логирование пропущенных профилей (mode: local, missing fields)
- ✅ Логирование ошибок валидации (invalid port, etc.)
- ✅ Логирование выбора default профиля

#### 4. `index.ts`:
- ✅ Логирование проверки SSH_PROFILES_FILE
- ✅ Логирование загрузки профилей при старте

### 🔍 Что показывают логи:

Все логи имеют префикс `[Component Name]` для идентификации источника:
- `[MCP Server]` - начальная загрузка сервера
- `[Profiles File]` - загрузка файла профилей
- `[Profile Resolver]` - разрешение SSH конфигурации
- `[SSH Executor]` - SSH подключение и выполнение команд

Логирование уровней:
- `debug` - детальная информация (все шаги процесса)
- `info` - важные события (✅ успешные операции)
- `warn` - предупреждения (⚠️ потенциальные проблемы)
- `error` - ошибки (❌ критические проблемы)

### 📋 Для включения детальных логов:

**Важно:** Логи выводятся в stderr MCP сервера. Для просмотра логов нужно:
1. Открыть консоль/терминал где запущен Cursor
2. Или проверить логи MCP сервера в настройках Cursor

**Настройка в mcp.json:**
```json
{
  "mcpServers": {
    "ssh": {
      "command": "node",
      "args": ["/path/to/ssh-mcp-server/dist/index.js"],
      "env": {
        "SSH_PROFILES_FILE": "~/.cursor/ssh-profiles.json",
        "LOG_LEVEL": "debug"
      }
    }
  }
}
```

**После изменения mcp.json:**
1. Перезапустить Cursor полностью
2. Выполнить тестовую команду
3. Проверить логи в stderr (консоль Cursor или логи MCP)

**Что искать в логах:**
- `[Profiles File]` - загрузка профилей
- `[Profile Resolver]` - разрешение SSH конфигурации
- `[SSH Executor]` - детали подключения (ключ, путь, ошибки)
- `[SSH Executor] ❌` - ошибки подключения с деталями
