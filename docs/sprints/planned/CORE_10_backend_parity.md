# CORE_10 — безопасность подстановки, сетка бэкендов и флип

**Статус:** 🔵 ГОТОВ К РАБОТЕ
**Ветка:** `feat/openssh-transport` (продолжение CORE_08 и CORE_09)
**Предшественники:** [CORE_09](CORE_09_transport_safety.md) закрыт, [CORE_08](CORE_08_openssh_transport.md) ждёт шагов 6–7
**Блокирует:** переключение дефолтного бэкенда на openssh и удаление ssh2

---

## Зачем этот спринт

CORE_09 закрыл потерю и порчу данных. Осталось три вещи, без которых нельзя переключать
дефолтный бэкенд:

1. **Дыра в безопасности**, которая старше обоих спринтов: значение параметра инструмента
   попадает в строку команды и исполняется на сервере.
2. **Передачу файлов не проверяет ничто.** Контракт покрывает только `exec`, тесты
   инструментов мокают транспорт целиком, тесты раннеров смотрят на аргументы `scp`, а не
   на то, что оказалось на диске. 461 зелёный тест при живом блокере 1.2 — портрет этой дыры.
3. **Пределы**, часть которых выстрелит ровно в момент флипа.

Порядок работ: сначала безопасность (она ни от чего не зависит и дешёвая), потом сетка,
потом пределы, потом сам флип.

**Правило спринта, унаследованное от CORE_09:** каждый пункт, который трогает команды на
сервере, проверяется на живых контейнерах, а не только моками. В CORE_09 живая проверка
поймала четыре дефекта, которые не ловились тестами вообще.

---

## 📊 ПРОГРЕСС

### Этап 1 — безопасность подстановки (первым делом)

- [x] **1.1. Значение параметра исполняется на сервере** ✅ *(2026-08-03, было 1.4 в CORE_09)*
  - **Решено (2026-08-03):** чиним здесь, отдельный патч к 1.x не выпускаем — аудитория
    первой версии это мы сами, обновятся вместе с мажорным релизом.

  **Аудит проведён (2026-08-03).** Прошли по всем семи инструментам и внутренним модулям.
  Дыр не одна, а восемь в четырёх файлах, и `pattern` из них не самая опасная:

  | Место | Параметр | Что исполнится |
  |---|---|---|
  | `file-tools.ts:687` | `pattern` | `ls '/tmp'/*; rm -rf /tmp/x` |
  | `file-tools.ts:511` | `mode` | `chmod 644; id -- '/tmp/f'` |
  | `transfer-tool.ts:364,569` | `mode` | то же при загрузке файла и каталога |
  | `transfer-tool.ts:414-417,430` | `mode`, `owner` | `install -m … -o … -g …`, под **sudo** |
  | `log-tools.ts:162,189,277` | `lines`, `context` | `tail -n 100; id`, `grep -C 0; id` |
  | `audit-tool.ts:578,587,588` | `top_n` | `head -20; id` |
  | `audit-tool.ts:578` | `paths` | `echo "…du_'$(id)'…"` — `shellQuote` не спасает: одинарные кавычки попадают внутрь двойных, `$( )` отрабатывает |
  | `audit-tool.ts:620` | `log_lines` | `journalctl -n 50; id` |

  Чисто: `ssh_snapshot` (команды константные), `ssh_monitor` (команд нет), `domain` и `unit`
  в audit-tool (проверены regex), `cwd` в exec-tool (через `escapeShell`).
  `ssh_exec.command` — по определению исполнение, не дефект.

  **Тип из схемы не защищает.** MCP SDK проверяет конверт запроса, а `arguments` отдаёт как
  есть: `number` в `inputSchema` ничего не гарантирует, а код везде пишет `args.lines || 100`
  без приведения. Строка `"100; id"` доедет до команды.

  - Правка одна общая: модуль `src/utils/shell-arg.ts` — единственный законный способ
    вставить значение в команду. `count` (числа), `mode` (восьмеричные права), `owner`,
    `glob` (для `pattern`: `* ? [ ]` живут, остальное экранируется обратным слэшем —
    закавычить нельзя, раскрытие шаблона это заявленная функция инструмента).
  - **Решения по ходу (2026-08-03):** плохое значение — отказ с внятным текстом, не тихая
    санитизация; путь из разделителя секций в `ssh_disk_breakdown` убираем совсем
    (нумеруем `du_0`, `du_1`) вместо попыток его экранировать.
  - Проверка: тесты с `;`, кавычками, `$(…)` и обратными кавычками в значении — для
    каждого из восьми мест; живой прогон на обоих контейнерах.

  **Сделано.** `src/utils/shell-arg.ts` (`shellCount`, `shellMode`, `shellOwner`, `shellGlob`),
  врезан во все восемь мест. Права и владелец проверяются до первой команды — отказ на
  полпути оставил бы после себя временный путь. Тесты: `shell-arg.test.ts` (41) и
  `tool-arg-injection.test.ts` (14), счёт 461 → 516.

  Живой прогон на Alpine и Debian (`check-glob-live.mjs`): `*.log`, `my file*.log`,
  `журнал*.log`, `*;name.log` находят ровно свои файлы; `a.log; id`, `a.log; touch …/pwned`,
  `*.log$(id)` и `` *`id`.log `` уходят на сервер экранированными, `uid=` в выводе нет,
  файла `pwned` не появилось. Поведение обоих наборов утилит одинаковое.

- [x] **1.2. Единый способ раскрытия пути для чтения и списка** ✅ *(2026-08-03, хвост 1.3 из CORE_09)*
  - Запись уже раскрывает `~` у нас, по домашнему каталогу из паспорта. Чтение и список
    остались на старом способе (`expandRemoteTilde` → `$HOME` в двойных кавычках), то есть
    в одном инструменте живут два разных правила.
  - Задача: свести к одному способу; `escapeForDoubleQuotes` после этого, скорее всего,
    станет не нужен.
  - **Найдено при аудите 1.1 (2026-08-03), оба дефекта в старом способе:**
    - `escapeForDoubleQuotes` превращает `!` в `\!` — защита от history expansion, которой
      в неинтерактивном shell нет. В двойных кавычках обратный слэш перед `!` не съедается,
      поэтому путь `~/файл!` читается как `файл\!` — открывается не тот файл (или ничего).
    - `~user/path` заворачивается в одинарные кавычки и тильда не раскрывается вовсе, хотя
      комментарий в коде обещает «shell will expand». В записи такой путь уже отклоняется
      явной ошибкой — чтение и список молчат.
  **Сделано.** `src/managers/remote-home.ts` — одна функция `expandRemoteHome` на все
  инструменты: раскрывает `~` по домашнему каталогу из паспорта (одна проба за сессию, дальше
  кэш), путь уезжает в одинарных кавычках. Врезана в запись, чтение, список и оба журнальных
  инструмента. Пути без тильды паспорт не запрашивают — лишнего RTT нет.

  Вместе со старым способом ушли: `expandRemoteTilde`, `escapeForSingleQuotes`,
  `escapeForDoubleQuotes`, `escapePath`, `buildSafeCommand` (из `file-tools.ts` и
  `log-tools.ts`) — они существовали только ради `$HOME` в двойных кавычках.

  Под sudo `~` ведёт в дом логин-пользователя; при записи это сказано в ответе, при чтении —
  в журнале: ответ чтения это содержимое файла, подмешивать в него предупреждение нельзя.

  Тесты: `remote-tilde.test.ts` (8), счёт 516 → 525. Живой прогон (`check-tilde-live.mjs`) на
  Alpine и Debian, под `root` и `deploy`: читаются `файл!.txt`, `dollar$var.txt`,
  `` tick`id`.txt ``, `it's.txt`, `sp ace.txt`; список и `tail` идут тем же путём; под sudo
  `~` ведёт в `/home/deploy`; `~postgres/data` отклоняется с текстом.

### Этап 2 — сетка, которая ловит расхождение бэкендов

- [ ] **2.1. Контракт `CommandRunner` должен покрывать передачу** 🔴
  - Задача: добавить в общий набор (`tests/unit/runner-contract.ts`) проверки
    `upload`/`download`, включая рекурсивную, и прогонять их на обоих бэкендах.
    Ключевое — сформулировать, что значит «положить каталог X в Y» (см. 2.3): контракт,
    который об этом молчит, — не контракт.
  - Формулировка из скетча CORE_09 («Контракт путей передачи»): транспорту всегда даётся
    **несуществующая** цель, и он обязан её создать; существующая цель — нарушение
    контракта, а не «положим внутрь».

- [ ] **2.2. Тест против настоящего ssh/scp, а не против мока** 🔴
  - Задача: прогон передачи файла и каталога на реальном транспорте — контейнер или
    localhost. Минимум сценариев: каталог с подкаталогами, имя с пробелом, имя с
    кириллицей, пустой каталог, замена существующего каталога.
  - Сюда же — прогон на образе с BusyBox и на образе с dash.

- [ ] **2.3. Рекурсивная передача кладёт дерево не туда** 🔴 *(было 1.2 в CORE_09)*
  - Сейчас: инструмент создаёт staging, затем зовёт `runner.upload(localDir, stagingDir,
    {recursive:true})`. На openssh это `scp -r localDir host:stagingDir`, а scp в
    **существующий** каталог создаёт `stagingDir/<имя каталога>/…`. Бэкенд ssh2 кладёт
    содержимое прямо в цель. После флипа: с `verify` — падение, без `verify` — молча лишний
    уровень вложенности на боевом месте.
  - Отдельно проверить ту же ловушку на скачивании.
  - Проверка: тест против настоящего `scp` (пункт 2.2), а не против мока.

- [ ] **2.4. Симлинки внутри дерева** 🟡
  - Контракт объявлен в скетче CORE_09 (ссылка на файл разыменовывается, битая
    пропускается с предупреждением, цикл прерывает операцию), но обход дерева его не
    исполняет: наш обход их пропускает, scp разыменовывает, ssh2 не передаёт вовсе —
    три разных поведения на один вызов.

- [ ] **2.5. Правка профиля не доходит до транспорта** 🟡 *(было 2.5 в CORE_09)*
  - Сейчас: `get-runner.ts:59-64` кэширует `Ssh2Runner` с конфигом первого вызова навсегда.
    `ssh_monitor reload` перечитает файл профилей, но команды продолжат ходить по старому
    адресу и ключу до перезапуска процесса. На openssh это решено отпечатком конфига,
    на **дефолтном** бэкенде — регресс относительно старого пула.
  - Проверка: тест «сменили host в профиле → следующая команда идёт на новый host».

### Этап 3 — пределы и ресурсы

- [ ] **3.1. Жёсткий потолок 300 секунд на любую передачу** 🔴 *(выстрелит на флипе)*
  - `DEFAULT_TRANSFER_TIMEOUT_MS` нечем переопределить — инструмент не передаёт `timeoutMs`,
    в схемах `ssh_upload`/`ssh_download` параметра таймаута нет вовсе. Каталог на гигабайты
    или файл по медленному каналу гарантированно оборвётся на 301-й секунде, посреди
    staging-а. Старый код не имел таймаута совсем; менять «висит вечно» на «всегда умирает
    через 5 минут» — тоже регресс.

- [ ] **3.2. Одновременное открытие всех файлов каталога** 🟡
  - `Promise.all(files.map(sha256OfFile))` открывает все файлы дерева разом — на тысячах
    файлов упирается в лимит дескрипторов (EMFILE).

- [ ] **3.3. Короткий пароль портит вывод команд** 🟡
  - `redactSecret` вырезает секрет из вывода **каждой** команды парольного профиля. Пароль
    вида `root` или `test` превратит все легитимные вхождения этой подстроки в `***` —
    `ssh_file_read` конфига вернёт испорченный текст, и человек об этом не узнает.

- [ ] **3.4. Путь scp на старых клиентах проходит через shell** 🟡
  - **Пересмотрено экспериментом (CORE_09):** шаблоны раскрываются **в обеих** версиях
    клиента, а не только в 8.x — скачивание `star*name.txt` притащило три посторонних файла
    и на клиенте 10.2. Значит защита нужна всегда. Одинарные кавычки не работают ни в одном
    режиме; работает **экранирование обратным слэшем** — проверено для пробела и звёздочки.
  - Страховка от конструкции: временные имена придумываем мы, поэтому в scp попадают только
    безопасные символы, а боевое имя появляется в `mv`, где экранирование надёжно.

- [ ] **3.5. Зависшая передача без таймаута** 🟡 *(было 2а.3)*
  - Закрыто транспортом в спринте 8, фиксируем тестом в общей сетке, чтобы не потерять.

### Этап 4 — живучесть и жалобы с боевого использования

- [ ] **4.1. Собрать фактические ошибки** 🔴 *(было 2а.4, ждёт пользователя)*
  - Нужны от пользователя тексты ошибок и логи сессии, где падали `ssh_upload`,
    `ssh_download`, `ssh_file_write`.
  - **Одна причина, похоже, уже найдена попутно:** быстрый путь записи портил содержимое
    (апостроф → пять символов), и это объясняет, почему агенты уходили к `ssh_exec` с
    ручным heredoc. Но это догадка — логи скажут, была ли она единственной.

- [ ] **4.2. Схема `ssh_exec` использует `oneOf`** 🟡 *(было 2а.2)*
  - `command` описан через `oneOf` строки и массива. Часть клиентов такие схемы
    переваривает плохо — отсюда «при передаче параметров падает». Косвенное подтверждение:
    в проекте есть модуль `array-validator.ts`, написанный ради ловли строк вида `['a','b']`.
  - Имена и смысл параметров не менять — пакет опубликован.

### Этап 5 — флип и уборка

- [ ] **5.1. Живой смоук перед флипом** 🟡
  - Повторить проверку из шага 3 CORE_08 (6 команд → один вход в журнале) уже после врезки
    транспорта, на боевых серверах, плюс сценарий с **парольным профилем** — его живьём не
    проверяли ни разу, только юнитами.

- [ ] **5.2. Флип дефолта на openssh** 🔴 — шаг 6 из CORE_08.

- [ ] **5.3. Удаление ssh2** 🔴 — шаг 7 из CORE_08. Вместе с ним умрут: пул соединений,
  обработчик SFTP-канала, половина дублей дефолтов, часть реализаций `shellQuote`.

- [ ] **5.4. Документация мажорного релиза** 🔴
  - Миграционная секция: изменения поведения, накопленные в CORE_09 (см. ниже) и в этом
    спринте.
  - `docs/transfer.md` описывает старую механику (пул, fastPut, 32 КБ-чанки) — переписать.
  - **Описание инструмента разошлось с поведением:** схема `ssh_file_write` и README
    говорят «`binary` — uploaded via SFTP», а мелкое двоичное содержимое теперь едет в
    stdin. Результат тот же байт в байт, но документация врёт.

---

## Изменения поведения для миграционной секции

Накоплено в CORE_09, вписать в релиз-ноты мажорной версии:

- `atomic: false` для каталога больше не означает «лить прямо в боевой путь» — данные
  всегда едут через временный путь. Флаг сохраняется в ответе, поведение одно.
- Появились отказы там, где раньше была тихая порча: файл поверх каталога, симлинк на месте
  цели, точка монтирования.
- `~` под sudo ведёт в дом **логин-пользователя**, а не root (раньше тильду раскрывал
  сервер уже под sudo). В ответе об этом сказано.
- `~user/` отклоняется; на сервере, который не сообщил домашний каталог, `~`-путь тоже
  отклоняется — вместо записи наугад.
- `ssh_file_read` при обрезке вывода **отказывает** и предлагает `ssh_download` или чтение
  по диапазону строк.
- Сообщения об ошибке стали длиннее: они несут адреса оставшихся копий и найденных рядом
  следов прерванных операций.

Добавлено в CORE_10:

- **Значения параметров проверяются до отправки команды.** `mode`, `owner`, `pattern`,
  `lines`, `context`, `top_n`, `log_lines` с посторонним содержимым отклоняются с текстом
  вида `mode must be an octal permission like 644 or a symbolic one like u+x, got "644; id"`.
  Раньше такое значение уезжало в команду: часть срабатывала как команда, часть — как
  невнятная ошибка утилиты. Рабочие формы сохранены целиком: `644` и `u+x` для прав,
  число строкой для счётчиков, `*.log` и имена с пробелом, кириллицей, точкой с запятой
  для шаблона.

---

## Потенциальные баги — найдены, но не подтверждены и не закрыты

Перенесено из CORE_09 плюс найденное в конце спринта. Порядок — по цене ошибки.

- **Точка монтирования: поведение не измерено.** Установщик отказывается заменять путь на
  отдельной файловой системе, но само поведение `rm -rf` и `mv` на mountpoint проверить не
  удалось — в контейнерах нет прав на `mount`. Отказ построен на рассуждении и сравнении
  номеров устройств через `stat -c %d`. Отдельно: `stat -c` — синтаксис GNU и BusyBox; на
  BSD и macOS нужен `stat -f`, и там проверка молча отвечает «не знаю» и не блокирует.

- **Гонка двух установок в один путь.** Проверено вживую: одна коммитит, вторая отказывает,
  оба дерева целы — приемлемо. Плохо другое: проигравший получает сырой текст `mv`
  («cannot stat»), из которого не понять, что произошло. Нужен внятный ответ «в этот путь
  одновременно писал кто-то ещё, ничего не изменено». Замок через `mkdir` возможен
  (проверено), но добавляет состояние, которое надо снимать после аварий.

- **Отмена операции существует только на бумаге.** `AbortSignal` доходит до транспорта и
  установщика, но инструменты его не принимают — практический эквивалент отмены сегодня
  это убийство процесса сервера.

- **Команда, убитая по таймауту, возвращается на 5 секунд позже срока.** Локальный `ssh`
  при мультиплексировании не умирает от SIGTERM и уходит только по SIGKILL после паузы.
  Замерено: таймаут 3 с → ошибка через 8 с.

- **Межфайловый `mv` — это копирование, и два набора утилит расходятся.** Через границу
  файловой системы на существующий пустой каталог coreutils копирует и возвращает 0, а
  BusyBox отказывает. Плюс копирование не атомарно: при нехватке места приёмник остаётся
  частичным. Установщик защищён тем, что временный путь всегда рядом с целью, — но если
  появится ветка, кладущая staging в `/tmp`, расхождение выстрелит.

- **Поиск следов прерванных операций не проверяет обрезку вывода.** В каталоге с сотней
  тысяч файлов список может прийти неполным, и мы назовём не все артефакты. Последствие
  мягкое (мы ничего не удаляем), но неполнота молчаливая.

- **Установка стоит на один RTT дороже, чем нужна.** Поиск следов идёт отдельной командой
  от разведки типа цели — сливается в одну. Отдельно: для записи мелкого файла хвост
  `cat > staging && chmod && mv -T` можно слать одной командой, не нарушая инвариант
  (переименование остаётся последним), — 4 RTT превратятся в 2.

- **Накопление своего мусора на маленьких дисках.** `.upload-*` мы сознательно не убираем
  автоматически (см. ADR 001), поэтому при частых обрывах на роутере с маленьким флешем
  они будут копиться. Сейчас компенсируется тем, что каждый вызов их называет в ответе.

---

## С чего начать

1. Прочитать этот файл целиком.
2. Прочитать `docs/decisions/001-single-install-point.md` — правила установщика, ломать
   которые нельзя.
3. Прочитать «Итоги» в [CORE_09](CORE_09_transport_safety.md) — что уже закрыто и почему
   именно так; спорить заново не о чем.
4. Начать с пункта 1.1: сначала проход по всем инструментам, потом один общий способ
   ограждения. Код до аудита не писать.

**Лаборатория.** Контейнеры `mcp-alpine` (порт 2231, BusyBox, bash нет) и `mcp-debian`
(порт 2232, coreutils, sh это dash), пользователь `root`, плюс непривилегированный `deploy`
с NOPASSWD-sudo. Ключ и скрипты живых проверок — в scratchpad `ssh-lab/`. Снести:
`docker rm -f mcp-alpine mcp-debian`.

- `SSH_MCP_CONTROL_DIR` для живых прогонов обязан быть коротким (`/tmp/mcp-lab-ctl`) —
  лимит сокета 104 байта.
- Живой скрипт запускать в фоне с выводом в файл: процесс не всегда завершается сам.
- `--` допустим в `mv`, `rm`, `cp`, `mkdir`, `stat`, но **не в `test`**.
- Переименования только `mv -T`.
