# CORE_11 — один транспорт вместо двух

**Статус:** ✅ ЗАКРЫТ — чанки 1–5 закрыты, `ssh2` снесён, ревью ветки пройдено
и найденные им дефекты починены
**Ветка:** `feat/openssh-transport` (продолжение [CORE_10](CORE_10_backend_parity.md))
**Предшественник:** CORE_10, этапы 1–4 закрыты; его 5.2 и 5.3 закрыты здесь
**Дальше:** [CORE_12](CORE_12_release_v2.md) — документация и релиз

---

## Зачем этот спринт

В проекте живут два способа ходить по SSH: библиотека `ssh2` и системный `ssh`. Второй
проверен живьём и умеет всё, что умеет первый. Пока их двое, каждая правка делается дважды,
а каждый тест доказывает поведение только одной половины.

Спринт оставляет один транспорт и убирает следы второго.

**Точка невозврата — снос `ssh2`.** После него вернуться можно только откатом коммита,
поэтому порядок работ такой: сначала переключить дефолт и убедиться, что всё живо, и
только потом удалять.

**Рамка сверх этого.** Сервер должен ходить не только на свежую Ubuntu, но и на роутер, где
половина протоколов не собрана. Поэтому поддержка старых серверов живёт **слоем, а не ветками
`if` по коду**. Основа уже есть: `runtime-check.ts` знает, что умеет клиент, `passport.ts` —
что умеет сервер, `ssh-args.ts` выбирает способ подготовки пути по режиму клиента. Всё новое
ложится туда же.

---

## Решения, принятые до старта

- **Дверь к транспорту остаётся.** `get-runner.ts` не удаляем: логики в нём после сноса нет,
  но это единственная точка, где инструменты берут соединение. Когда появится слой для старых
  серверов, развилка встанет туда, а не в четыре инструмента.
- **На выходе соединения не закрываем.** Сокет общий для машины: закрытие рвало бы канал
  соседнему окну и стоило бы полного входа при каждом перезапуске. Но оставленное показываем
  и срок простоя называем — см. 2.3.
- **Срок простоя задаётся переменной окружения, не параметром вызова.** Срок прибивается к
  соединению в момент подъёма, поэтому число из второго вызова ничего бы не изменило —
  получилось бы обещание, которое некому выполнить.

---

## Как идём

Чанками, по одной задаче за раз, с паузой после каждой. Точка отсчёта: 804 юнита, 142 живых,
`tsc` чист. На конце чанка 1: 845 юнитов, 172 живых, `tsc` чист.

---

## Чанк 1 — правила путей и флип дефолта

- [x] **1.0. Разбор спринта по чанкам** — этот файл.

- [x] **1.1. Правила путей обходятся в журнальных инструментах** 🔴 — *сделано (2026-08-05)*
  - **Замер до правки подтвердил и оказался хуже ожидания:** `ssh_log_tail ~/secret` вернул
    содержимое запрещённого файла на обоих контейнерах, `ssh_log_search` нашёл в нём строку.
    Тот же файл по пути `/root/secret` отклоняется. Файловые инструменты отклоняют оба вида.
  - **Сделано.** `buildSafePath` в `log-tools.ts` зовёт `resolveRemotePath` вместо
    `expandRemoteHome`, два блока проверки сырого пути убраны вместе с импортом
    `createPathValidator`. Нового кода нет — подключена функция, написанная в 3.6 ровно
    от этого дефекта.
  - **Побочно:** в списке путей запрет теперь отказывает только своему пути, остальные логи
    читаются (раньше один запрещённый ронял весь вызов).
  - **Правила профиля живьём не проверял никто** — ни одного теста с `pathSecurity` в
    `tests/live/`. Отсюда и прожило: юниты мокают транспорт. Заведён
    `tests/live/path-rules.live.test.ts` (8 утверждений на двух серверах), где проверяется
    утечка содержимого, а не текст ошибки.
  - Тесты: +5 юнитов в `path-security-tilde.test.ts` (обе ветки каждого инструмента: один
    путь и список), 804 → 809; живых 142 → 150. Негативный контроль: возврат
    `expandRemoteHome` роняет 4 юнита и 4 живых на обоих контейнерах, а файловый инструмент
    и разрешённый путь остаются зелёными.

<details>
<summary>Исходная формулировка</summary>
  - `ssh_log_tail` и `ssh_log_search` проверяют правила профиля на **сыром** пути, а `~`
    раскрывают уже после: `log-tools.ts:154-162` и `:268-275`, раскрытие на `:166` и `:287`.
  - Валидатор на тильде подставляет выдуманный дом (`path-validator.ts:153-157` →
    `/home/user/…`), поэтому при `deniedPaths: ['/root']` под root путь `~/secret` запрет
    проходит. Тот же класс, что чинили в 3.6, — там порядок исправили в файловых инструментах
    и передаче, журнальные пропустили.
  - Замер сначала: профиль с `deniedPaths: ['/root']`, `ssh_log_tail ~/secret` под root на
    обоих контейнерах. Не подтвердится — пункт снимается.
  - Правка: перевести оба места на `resolveRemotePath` (`remote-home.ts:81`) — он уже делает
    «раскрыть → проверить» и стоит в `file-tools.ts:238,375,627` и `transfer-tool.ts:257,687`.
    Нового кода не пишем.
  - Тесты: юниты рядом с `path-security-tilde.test.ts` (он покрывает пять инструментов,
    журнальные — мимо) плюс живое утверждение. Негативный контроль обязателен.
</details>

- [x] **1.1б. Валидатор угадывает дом вместо отказа судить** 🟡 — *сделано (2026-08-05)*
  - **Сделано.** Подстановка `/home/user` из `normalizePath` убрана целиком. Путь с `~`
    отклоняется, но **только когда есть с чем сравнивать**: при одних
    `maxPathLength`/`allowTraversal` списков префиксов нет, значит и отказывать не за что —
    тильда проходит. (Текст отказа и остаток нормализации переписаны в 1.1в.)
  - Чужой дом (`~deploy/…`) попадает под то же правило: раскрыть его нечем, судить тем более.
  - Наблюдаемое поведение на сервере не менялось: единственный вызывающий в `src` —
    `remote-home.ts:89`, он зовёт валидатор уже после раскрытия, и тильда до него не доходит
    (`~user/…` отклоняет ещё `expandRemoteHome`). Правится контракт класса, чтобы следующий
    вызывающий не наступил на 1.1 в новом инструменте. Доказательство — живая сетка 150/150
    зелёная без единой правки.
  - Тесты: блок «Tilde path normalization» в `path-security.test.ts` описывал ровно угадывание
    и заменён на шесть утверждений нового поведения, 809 → 814. Негативный контроль на обе
    половины условия: убрать отказ — краснеют три; убрать «есть с чем сравнивать» — краснеют
    три другие.
  - Относительный путь без тильды остался мимо правил — вынесено в 1.1в.

- [x] **1.1в. Правила сравнивают сырую строку — шесть обходов вместо одного** 🔴 — *сделано (2026-08-05)*
  - **Замер до правки** (профиль с `deniedPaths: ['/root']` или `allowedPaths`): запрет
    обходят `..` из разрешённого каталога, сдвоенный слэш, относительный путь и символическая
    ссылка наружу; белый список вдобавок пропускает соседа по имени (`/var/logs-of-someone-else`
    при правиле `/var/log`), а запрет цепляет непричастный `/rootkit`. Шесть строк, один класс:
    сравнивалась строка, как её передал вызывающий.
  - **Замер на трёх узлах** (включая роутер): `readlink` и `realpath` есть везде; `pwd` совпадает
    с `$HOME`, значит относительный путь достраивается от дома; `readlink -f` молчит, если нет
    каталога в середине пути; `realpath -m` есть только у coreutils; BusyBox отдаёт корень
    сдвоенным слэшем. Отсюда способ резолва: снять хвост до существующего предка, резолвить
    предка, вернуть хвост на место.
  - **Сделано.** Новый `src/managers/path-guard.ts` — единственная дверь: три источника знания
    (локальная лексика → дом из паспорта → назначение ссылок с сервера) и четыре исхода
    (`ok`, `rewritten`, `unverified`, `denied`). `remote-home.ts` переехал в него целиком.
    Валидатор судит только канонический путь и сравнивает по границе каталога; угадывать он
    больше не умеет вовсе. Правило применяется дважды — к имени и к назначению.
  - **Цена.** Проба на сервер — только у профилей с правилами; без `pathSecurity` не задаётся
    ни одного вопроса. Живая сетка идёт те же 52 секунды.
  - **В команду уезжает путь с раскрытой тильдой, не свёрнутый.** `..` считается после перехода
    по ссылке: на лаборатории `/tmp/probe/link/../x` сервер понимает как `/x`, а лексическая
    свёртка даёт `/tmp/probe/x`. Канонический вид служит суждению, операция идёт по адресу
    вызывающего.
  - **Мутации нашли дефект в свежем коде:** при пустом доме относительный путь достраивался от
    корня — то же угадывание, только новое. Теперь такой путь каноническим не становится, и
    правило честно отвечает «судить нечем».
  - Тесты: `tests/unit/path-guard.test.ts` (18 утверждений, среди них исходы, которые живьём не
    воспроизвести: сервер без `readlink` и сдвоенный слэш BusyBox), переписан блок валидатора в
    `path-security.test.ts`; живой файл вырос с 8 до 26 утверждений — все шесть обходов плюс
    обратная сторона (ссылка внутрь разрешённого, `..` внутри него же, запись в ещё не созданное
    дерево). Юниты 814 → 837, живых 150 → 168.
  - Негативный контроль по слоям: убрать вопрос серверу — краснеют 2; не сворачивать путь —
    краснеют 2; сравнивать строкой вместо границы каталога — краснеют 2. Мутации по guard:
    89%, выжившие — тело `sh`-команды (умирает только живьём) и эквивалентные.
  - README: описано, как судится путь, и две границы — сервер без `readlink` работает с
    пометкой «проверено по имени», а `ssh_exec` правилами не покрыт, потому что команда не путь.

- [x] **1.2. Флип дефолта на системный `ssh`** 🔴 — *сделано (2026-08-05)*
  - `DEFAULT_BACKEND` в `get-runner.ts` — `openssh`; шапка файла переписана по факту.
    Текст предупреждения о непонятном значении переменной правки не потребовал: он
    собирается из самой константы. `ssh2` остался допустимым значением переменной.
  - `runner-factory.test.ts` — ожидания перевёрнуты. Три теста про реестр транспортов
    (переиспользование по имени профиля, разные профили, смена бэкенда) просят `ssh2`
    явно: реестр по имени профиля — свойство именно того бэкенда, у системного `ssh`
    транспорт живёт по конфигурации.
  - Тексты про таймаут правки не потребовали и стали правдой: `exec-tool.ts` обещает
    30000 мс, столько же у `openssh-runner.ts`; схемы `ssh_upload`/`ssh_download`
    обещают отсутствие потолка, и у системного клиента его нет. 3.1б в CORE_10 закрыт.
  - **Живая сетка от дефолта зависела, но ровно одним файлом.** Транспорт сменился только
    у `path-rules.live.test.ts`: он ходит через инструменты, то есть через `get-runner.ts`,
    и до флипа шёл прежним бэкендом. Проверено по логу — появилась строка
    `master connection closed`, и все 168 живых остались зелёными. Остальные девять файлов
    создают раннер сами: шесть ставят `SSH_MCP_BACKEND=openssh`, а `transfer.live`,
    `password-auth.live` и `output-secrets.live` зовут `getOpenSshRunner` напрямую.
  - Первая версия этой записи называла три флип-зависимых файла вместо одного: я взял
    ответ грепом по строке `SSH_MCP_BACKEND` вместо того, чтобы посмотреть, откуда файл
    берёт транспорт. Поймано ревью диффа, исправлено следующим коммитом.
  - **Флип ослабил существующий тест, и это показали мутации.** Пока дефолтом был `ssh2`,
    утверждение `resolveBackend(' openssh ') === 'openssh'` убивало поломку распознавания:
    сломанный разбор давал `ssh2`. После флипа тот же ответ приходит вторым путём — через
    ветку «значение непонятно, беру запасное», и мутанты `if (false)` в обеих проверках
    выжили заодно со всем логированием. Разницу видно только по предупреждению, теста на
    него не было. Дописаны четыре: молчание при отсутствии переменной, молчание на обоих
    узнанных значениях, текст предупреждения (называет и полученное значение, и запасной
    транспорт, и оба допустимых), одноразовость предупреждения.
  - Мутации `get-runner.ts`: 62.86% → 97.14%, выживших 13 → 1. Оставшийся — начальное
    значение флага «уже предупреждали»: общий `beforeEach` зовёт `resetRunnerRegistry()`,
    который выставляет то же самое, поэтому мутант неотличим.
  - Прогон: `tsc` чист, 841 юнит (837 + 4 новых), 168 живых на двух серверах, 52 с.
    Негативный контроль: возврат `DEFAULT_BACKEND` в `ssh2` даёт 4 красных из 8 в
    `runner-factory`, откат копией файла.
  - **README описывал прежний транспорт, и флип сделал это описание дефолтным.** Правлено
    по коду и живому контракту: передача идёт системным `scp` по общему соединению, а не
    SFTP поверх пула; `concurrency` помечен игнорируемым так же, как в схеме инструмента;
    ссылки в рекурсивной загрузке приезжают копиями, а не пропускаются; статистика
    `ssh_monitor` перечислена по полям `RunnerStats`, а не по счётчикам пула; переменные
    пула помечены как действующие только при `SSH_MCP_BACKEND=ssh2`, и сама переменная
    в README описана впервые.
  - Осталось неточным и уходит в вычитку CORE_12: схемы `ssh_file_write` / `ssh_file_read`
    говорят «via SFTP» (`file-tools.ts:54,79,98,128`). Это не ложь на клиенте 9.0+, где
    `scp` гоняет файлы поверх SFTP, но неточность на старых клиентах и на сервере без
    sftp. Текст схемы — публичный контракт, правится вместе с остальной вычиткой.

- [x] **1.3. Реестр долга — к факту** 🟢 — *сделано (2026-08-05)*
  - **Сверка разошлась с планом задачи в четырёх местах**, поэтому записано не то, что
    было намечено:
    - тильда в `remote_path` **уже была отмечена** закрытой (CORE_10, строка про 3.6);
      неотмеченным оставался один пункт — классический scp;
    - два долга из шести **уже записаны** в TD-03 (`isError` и частичный вывод убитой
      команды), оба со ссылкой на спринт миграции SDK. Заводить их вторым экземпляром
      значило бы развести два источника правды — записаны остальные четыре;
    - ссылки вида `remote-path-ops.ts:79` вели не туда: файл лежит в `src/managers/`,
      а не в `src/utils/`;
    - в списке потенциальных багов CORE_10 всё ещё числился догадывающийся о доме
      `PathValidator` — он закрыт задачей 1.1в этого спринта.
  - CORE_10 приведён к факту: классический режим закрыт живой проверкой
    (`tests/live/legacy-scp.live.test.ts`, 11 утверждений — счёт взят прогоном, не из
    памяти); догадка о доме помечена закрытой со ссылкой на ADR-004; «остальные поля
    профиля» помечены как задача 4.8, а не долг, чтобы не уехали в реестр дублем.
  - Записаны четыре долга, каждый проверен по коду перед записью: TD-06 (поиск следов не
    читает `truncated`), TD-07 (проигравшему в гонке установок уезжает текст `mv`), TD-08
    (`AbortSignal` есть в контракте транспорта и отсутствует в инструментах), TD-09
    (`stat -c` без ветки для BSD и macOS, плюс неизмеренное поведение на точке
    монтирования). Индекс `docs/tech-debt/README.md` пополнен.
  - Правок кода в задаче нет — только документация.
  - **Ревью диффа нашло у меня три ошибки, и одна вскрыла противоречие в CORE_10.** Я
    написал в TD-08, что отмену проводит установщик, — списал формулировку из списка
    потенциальных багов CORE_10 и не проверил. `AbortSignal` из установщика убран
    сознательно ещё в задаче 3.7 того же спринта, и `installer.ts` слова `signal` не
    содержит вовсе: два места одного документа противоречили друг другу, а я взял то,
    что попалось. Исправлены оба. Ещё две мелочи: диапазон строк `findLeftovers` был
    сдвинут на единицу, а ссылка «CORE_10, задача 7» вела в никуда — такой нумерации в
    спринте нет, номер был из моего рабочего счёта задач.
  - Пункты CORE_10, ставшие долгом, получили обратные ссылки на файлы реестра — чтобы
    следующий читатель не переписывал устаревшую формулировку оттуда, как это сделал я.

- [x] **Граница чанка 1: мутации по всему тронутому** 🟢 — *сделано (2026-08-05)*
  - Прогон от коммита перед чанком: `path-guard.ts` 89.26%, `path-validator.ts` 92%,
    `get-runner.ts` 97.14%. Общая цифра 48.51% — это `file-tools`, `log-tools` и
    `transfer-tool`, где чанк тронул одну строку импорта; их балл занижен, потому что
    они держатся на живых тестах, а мутации гоняют одни юниты.
  - **Мутант вскрыл регресс, внесённый в 1.1в: правила перестали пускать путь с именем
    `~`.** Условие `segment !== '~'` в `isCanonical` не защищало ни от чего — раскрывается
    только ведущая тильда, а её не бывает у пути, начинающегося со слэша, — зато отвергало
    законные `/tmp/~/x` и `/home/deploy/~`. Замер: оба получали «Path is not canonical».
    Такие имена на серверах есть, их оставлял прежний дефект загрузки, и убрать их при
    включённом `pathSecurity` стало нельзя — прямое сужение работающей функциональности.
    Условие снято, текст отказа поправлен.
  - Закрыты ещё две дыры, показанные мутантами: граница `maxPathLength` (путь ровно в
    предел проходит) и пустой белый список рядом с чёрным (это «список не задан», а не
    «запрещено всё» — иначе мутант запрещал бы вообще всё). Тексты двух отказов
    утверждаются целиком, а не по куску.
  - `path-validator.ts` 92% → 95.86%, выживших 12 → 6, все шесть эквивалентные: сравнение
    с корнем, две формы обрезки завершающего слэша, подстановка фиктивного пути в пустой
    чёрный список и два лишних `?.` там, где значение всегда задано.
  - Юниты 841 → 845, живых 168 → 172 (файл с именем `~` в разрешённом каталоге читается;
    тильда в середине запрещённого пути отказ не отменяет). Негативный контроль: возврат
    условия даёт 4 красных живых — по два на каждом сервере, откат копией файла.
    Следов на контейнерах не осталось.

---

## Чанк 2 — снос `ssh2` (точка невозврата)

- [x] **2.1. Удаление** — *сделано (2026-08-05)*
  - Удалены семь файлов (2301 строка) и зависимости `ssh2`, `@types/ssh2`.
  - **Сверка перед удалением: уникального не потеряно.** В `error-handling.test.ts` было
    22 утверждения — 15 проверяли `src/utils/retry.ts`, удаляемый той же задачей; пять
    (`:250-295`) сравнивали строки, которые тест собирал сам; два проверяли наличие методов
    у `SSHManager` и поля `pool.getStats()`. Ошибки системного клиента держит
    `error-classifier.test.ts` — **25 утверждений**, а не 18: цифра в плане была взята
    не прогоном.
  - **Удаление ломало компиляцию в четырёх местах**, из них два относились к задачам 2.2 и
    2.3: `index.ts:26,177-178`, `get-runner.ts`, `runner-factory.test.ts:20`,
    `transfer.live.test.ts:30`. Починены в этой же задаче — иначе прогон после удаления
    невозможен.
  - **`resolveBackend` снят вместе с механизмом выбора.** После сноса он обещал бы выбор,
    которого нет: переменная `SSH_MCP_BACKEND=ssh2` не изменила бы ничего. Реестр по имени
    профиля и `resetRunnerRegistry` ушли туда же.
  - **Имя профиля держалось только на этом реестре.** Параметр снят у `getRunner` и следом
    у `passport`, `testConnection`, `readFileBinary`, `putFile` — одиннадцать вызовов.
    Дальше по стеку цепочка осталась и ведёт в никуда — TD-10.
  - Тесты: −48 (три удалённых файла, счёт взят прогоном на прежнем коммите), −9
    `runner-factory` (тесты выбора бэкенда), −1 `monitoring-runner`, −1 `ssh-executor`.
    Живые: −26 (набор `ssh2` на двух серверах) и −2 (сверка бэкендов); набор `openssh`
    цел — 13 утверждений × 2 сервера.
  - **Итог по обеим задачам чанка:** `tsc` чист, юниты 847 → **806** (803 плюс три теста,
    дописанных по мутантам), живые 172 → **144**. Каждое слагаемое разницы названо выше;
    регресса среди них нет.

- [x] **2.2. Правки** — *сделано 2026-08-05*
  - ~~`get-runner.ts`~~, ~~`transfer.live.test.ts`~~ — сделаны в 2.1: без них не собиралось.
  - `types.ts:78` — `backend: 'openssh'`, пары больше нет. Поле осталось: его печатает
    `ssh_monitor`, а форму ответа инструмента не ломаем.
  - Тексты приведены к одному транспорту: `monitoring-tool.ts`, `runner-contract.ts`,
    `monitoring-runner.test.ts` (ожидание «pooled inside this process» заменено на
    `ControlPersist requires OpenSSH 5.6+`), `openssh-runner.test.ts:546`.
  - `transfer-contract.ts` — сняты `knownGaps` и обёртка `check`: тринадцать вызовов стали
    обычными `it(`. Сверять бэкенды больше не с чем, `record` необязателен.
  - Шесть живых файлов — строка `process.env.SSH_MCP_BACKEND = 'openssh'` убрана.
  - Частичный вывод убитой команды снос не задевает: `partialStdout`/`partialStderr`
    заполняет и системный клиент (`openssh-runner.ts:284,297`), а не только уходящий
    бэкенд. Поля остаются с источником, читать их по-прежнему некому — долг записан в
    `docs/tech-debt/archive/tool-schemas_03.md`.
  - Документы: `docs/tech-debt/test-coverage_01.md` (запись про `ssh-manager.ts` отпала
    вместе с файлом), CORE_10 пункт 5.3 отмечен со ссылкой на коммит `1031603`, README
    (раздел Transport, схема, переменные) и CHANGELOG (раздел про один транспорт с
    пометкой breaking). Полная вычитка — в CORE_12.
  - **Проверка.** `tsc` чист, юниты **806**, живые **144** на обоих контейнерах — те же
    числа, что после 2.1, правки ничего не отняли. Системным `grep` подтверждено: в
    `src` и `tests` слова `ssh2` нет, `SSH_MCP_BACKEND` остался только в истории спринтов
    и в записи CHANGELOG о ломающем изменении.

- [x] **2.3а. Наши следы на машине и срок простоя** — *сделано вместе с 2.1 (2026-08-05)*
  - **Замеры сделаны до кода, на обоих контейнерах, ответы совпали:**
    - `mtime` сокета командами **не обновляется** — это момент подъёма master, значит
      остаток срока вычислить нечем;
    - работа длиннее срока не рвётся: `ControlPersist=5`, команда 8 с — дошла;
    - убитый master оставляет **висячий сокет** (`-O check` → `Connection refused`),
      следующая команда молча поднимает соединение заново;
    - по истечении срока сокет удаляется сам;
    - проба сокета подключением безопасна: пять подряд, master тот же, команда после них
      проходит.
  - **Сделано.** `src/runner/control-sockets.ts` отвечает только фактами: какие сокеты лежат,
    когда подняты, живы ли (`alive` / `stale` / `unknown`). Текст собирают потребители —
    сейчас выход сервера, дальше `ssh_monitor`. Живость назначения осталась за `ssh -O check`
    в раннере: имя сокета — хэш, по нему сервер не восстановить.
  - **Остаток простоя не называем** — по инварианту честного ответа. Называем настроенный
    срок и момент подъёма.
  - `SSH_MCP_CONTROL_PERSIST` заведена здесь же: без неё «один источник» не собирался.
    Мёртвая опция `controlPersistSec` убрана — её не передавал никто, кроме теста.
  - Решение записано в [ADR-006](../../decisions/006-leftover-control-sockets.md).
  - Тесты: +8 юнитов на модуль (настоящие сокеты, не моки), +7 на разбор срока. Негативный
    контроль тремя мутациями: снятый фильтр префикса, потерянная классификация
    `ECONNREFUSED`, своё число вместо источника — каждая краснеет.
  - Замер печати на выходе: напечатанное совпало с содержимым каталога, срок приехал из
    переменной, master после выхода остался жив.
  - **Мутации дописали три теста.** Выжившие показали, что обход каталога молчит там, где
    должен говорить: имя без файла за ним (истёкший сокет) и непонятная ошибка на имени
    считались одним и тем же, а нечитаемый каталог выглядел как «соединений нет». Добавлены
    самоссылка (`ELOOP`), битая ссылка и каталог без прав; отдельно утверждается, что отказ
    по сроку называет пригодную форму значения. `control-sockets.ts` 78.95% → **94.74%**.
  - Осталось два выживших, оба названы: ветка таймаута пробы (`control-sockets.ts:51`) —
    сокет, принявший соединение и замолчавший, на unix-сокете дёшево не воспроизводится;
    начальное значение флага предупреждения (`ssh-args.ts:62`) — эквивалентный, `afterEach`
    выставляет то же самое.

- [x] **2.3б. Закрыть сейчас** — *сделано 2026-08-06*
  - `ssh -O exit` уже был в раннере (`openssh-runner.ts:173`), не хватало двери из
    инструмента и честного ответа: метод молчал, и «закрыл» от «закрывать было нечего»
    отличить было нечем. Теперь возвращает `closed` / `nothing-to-close` /
    `multiplexing-off` — три исхода, как того требует инвариант честного ответа.
  - Действие `close` работает по профилю, как `stats` и `test`. Иначе честно не выйдет:
    имя сокета — хэш `%C`, назначение по нему не восстановить, а удалить файл руками —
    это не закрытие, а видимость. Соседние профили остаются жить, и ответ называет,
    сколько их и на какой срок простоя.
  - **Найден и починен дефект рядом:** `handleCall` возвращал промис из `switch` без
    ожидания, поэтому отказ любого действия уходил мимо перехвата — вместо ответа с
    пометкой ошибки наружу летело исключение. Ловушка сработала на первом же тесте
    закрытия; починено разом для всех пяти действий.
  - Тесты: +8 юнитов на инструмент, +3 на `closeMaster` в раннере, новый живой файл
    `tests/live/close-connection.live.test.ts` (8 утверждений на двух контейнерах).
    Живой файл держит **свой** каталог управления: соседние живые наборы идут
    параллельно и держат сокеты в общем, счёт по нему был бы бессмысленным.
  - Состояние спрашивается у машины, а не у инструмента: сокет читается из каталога и
    опрашивается подключением напрямую, мимо наших модулей. Негативный контроль —
    мутация «рапортовать о закрытии, ничего не делая» роняет 7 живых утверждений из 8.
  - **Мутации.** Непокрытых в новом коде не осталось. Двое выживших дописаны тестами:
    запускается именно `ssh` и берётся конфигурация названного профиля, а не какая
    придётся. Оставшиеся трое названы и не чинятся: два текста в логах (логирование в
    проекте не проверяется нигде, кроме маскировки секретов) и `type: 'text'` в форме
    ответа MCP — соглашение всего файла, его проверка не относится к этой задаче.
  - Проверка: `tsc` чист, юниты 806 → **817**, живые 144 → **152**.

---

## Чанк 3 — дубли и сверка хешей

### Что здесь значит «свести дубль»

Дубль — это одно правило, записанное в нескольких местах. Пока его не трогают, всё
работает. Ломается при первой правке: меняешь значение в одном месте, остальные остаются
прежними, и код начинает противоречить сам себе — а тесты молчат, потому что каждое
значение по отдельности верное.

Свести — оставить одно место, где значение живёт, остальные заставить брать его оттуда.
**Поведение при этом не меняется:** 30000 остаётся 30000. Меняется только то, что теперь
его можно поправить в одной строке, а не в четырёх.

Три пункта ниже — не про удобство, а про настоящие дефекты: 3.3 (правило защиты имени
размножено втрое), 3.5 (длина считается не в тех единицах), 3.6 (обещанный срок
умножается на число файлов).

По одному пункту за подход, каждый со своим тестом.

- [x] **3.1. Таймаут выполнения записан четырьмя числами** — *сделано 2026-08-06*
  - **Число ушло из веток инструмента совсем, а не переехало в константу.** Замысел был заменить
    оба `args.timeout || 30000` на общую константу, но инструменту число там не нужно: ноль он и
    так читает как «срок не назван», а подставить общий срок умеет слой ниже. Осталось два места
    вместо четырёх — источник (`openssh-runner.ts:52`) и схема, — и связаны они стрелкой, а не
    копией. Поведение прежнее: нет параметра → 30000, `0` → 30000, `5000` → 5000.
  - `ssh-executor.ts:21` держит дверь к сроку для инструментов, чтобы `exec-tool` не тянулся
    через слой в раннер. Комментарий в `types.ts:13` перестал называть число сам.
  - Тест сравнивает **два независимых наблюдения**: `default` из схемы, как её отдаёт `getTool()`,
    и `timeoutMs`, реально уехавший в транспорт. Подменён только раннер, поэтому проверяется вся
    цепочка целиком. Юниты 817 → 823.
  - Негативный контроль: литерал `60000` в схеме роняет 4 утверждения из 5, пропуск нуля вниз —
    одно; откат копией файла.
  - **Мутации нашли два недописанных теста в свежем коде:** описание схемы врало бы про секунды
    (`/ 1000` → `* 1000` выживал, потому что тест смотрел только на миллисекунды), а названный
    срок проверялся лишь в одиночной ветке — вторая, батчевая, оставалась без сторожа. Оба
    дописаны, на тронутых строках выживших не осталось.
  - **Моки двух тестовых файлов подменяли исполнитель целиком и о новом экспорте не знали** —
    прогон упал сразу. Это не помеха, а свойство: мок обязан быть полным по контракту модуля.
- [x] **3.2. Предел вывода записан числом и строкой** — *сделано 2026-08-06*
  - Число живёт в `output-notes.ts` (`OUTPUT_LIMIT_BYTES`), метка считается из него функцией
    `byteLimitLabel`; `process.ts` берёт величину оттуда. Дом выбран по слоям: `utils` не
    импортирует из `runner` ни разу, обратное — штатное направление.
  - Тест связывает метку с числом, а число — с поведением: настоящий процесс печатает
    `OUTPUT_LIMIT_BYTES + 1` байт и обрезается ровно по пределу, а ровно предел проходит целиком.
    Юниты 823 → 834.
  - Негативный контроль: своё число в `process.ts` (8 МиБ вместо константы) роняет оба
    утверждения о поведении; откат копией файла.
  - **Мутации нашли дыру в старом накопителе:** `this.limit - this.size` → `+` выживал, потому что
    все тесты обрезки отдавали вывод одним куском, а при пустом буфере остаток равен всему
    пределу — разницы не видно. Дописан тест на две порции; он и краснеет на этой мутации.
  - `byteLimitLabel` покрыта целиком (границу единицы, дробь и величину выше GiB проверяют
    отдельные случаи); выживших на тронутых строках не осталось.
- [x] **3.3. Правило защиты имени для shell размножено втрое** — *сделано 2026-08-06*
  - Реализация одна: `utils/shell-arg.ts` (приём `'\''`). Копия в раннере и `escapeShell`
    в исполнителе удалены, десять файлов берут функцию оттуда. Приём `'"'"'` ушёл вместе с
    методом — в shell он давал тот же результат, но требовал доверия к третьему тексту.
  - **Живая проверка — на обоих контейнерах, всё семейство сразу:** пробел, апостроф, `$HOME`,
    обратная кавычка, обратный слэш, перевод строки, тильда, `*`, `; id ;`. Проверяется состояние
    сервера, а не текст ответа: сколько файлов появилось и что в них лежит. Имена при сверке не
    печатаются вовсе — их печать шла бы тем же способом, который и проверяем.
  - Отдельная проба на подстановку: имя содержит `$(touch <маркер>)`, и проверяется отсутствие
    маркера. Счёт файлов её не поймал бы — она создаёт файл в стороне, а не рядом.
  - **Путь `sudo` проверен отдельно:** команда уезжает внутрь `sudo sh -c '…'`, то есть кавычки
    накладываются дважды, и приём обязан пережить сам себя. Юнит проверяет то же на настоящем
    `/bin/sh` — оба круга, а не форму строки.
  - Юниты 834 → 855, живые 152 → 162 (12 файлов, оба контейнера).
  - Негативный контроль: `shellQuote` без экранирования апострофа роняет все 10 живых, включая
    обе пробы на подстановку — значит `$(…)` там действительно выполняется. Откат копией файла.
  - Один тест ждал старого приёма буквально (`ssh-executor.test.ts:130`) — ожидание переписано
    на `'\''`. Это единственное место, где смена приёма видна снаружи.
- [x] **3.4. `localFileSize` (`sha256.ts:77`) не вызывает никто** — ни `src`, ни тесты. ✅
  - Функция удалена вместе с осиротевшим импортом `stat` из `fs/promises`; `tsc --noEmit` чист,
    юниты 855 — столько же, сколько было: тестов у неё не было ни одного.
  - `npx knip` прогнан разово, в проект не поставлен. Пятнадцать имён, разобраны грепом на три
    класса: два настоящих мертвеца (`validateSSHConfig`, `profileDataToSSHConfig`), девять
    лишних `export` при живом коде, четыре особенности прогона (входная точка Stryker,
    `LAB_ROUTER` из 4.1, оснастка живых тестов). Всё — в [TD-11](../../tech-debt/unused-exports_11.md),
    не чиню здесь: пятнадцать чужих файлов в диффе задачи про одну функцию.
  - Находка, которую стоит знать: `profileDataToSSHConfig` собирает конфигурацию вручную,
    поле за полем, и `pathSecurity` в её список не попал. Сегодня она мертва, поэтому вреда
    нет; позовут по имени — правила путей потеряются снова.
  - Мутации по незакоммиченному: в `sha256.ts` один выживший, `new Array<string>(n)` →
    `new Array()`. Эквивалентный: длина массива тут ни на что не влияет, места заполняются
    по индексу.
- [x] **3.5. Длина команды считается в символах, предел сервера — в байтах.** ✅
  - **Замер потолка (оба контейнера, одинаково).** Команда длиной 131 000 байт доезжает,
    131 072 — уже нет: `/bin/sh: Argument list too long`, вывод пустой. Свыше четверти
    мегабайта сервер рвёт соединение. Число одно и то же для одной длинной лексемы и для
    сотни коротких: предел стоит на всей строке, а не на числе аргументов. `sudo` потолок
    не сдвигает.
  - **Замер занижения счёта.** На одном и том же чанке (95 имён, счёт 32 585): латиница —
    32 502 байта, кириллица — **62 902**, имена из апострофов — **123 702**, а под sudo те же
    апострофы дают **397 885** и соединение рвётся. То есть счёт в знаках занижал вес имени
    вдвое на кириллице и вчетверо на апострофах.
  - **Правка** (`remote-verify.ts`): `pathCost` считает `Buffer.byteLength` закавыченного
    имени, под sudo — закавыченного дважды, потому что исполнитель заворачивает всю команду
    в `sudo sh -c '…'`. Начало команды (`sha256sum -- ` или `openssl dgst -sha256 `) и
    обвязка sudo входят в бюджет чанка. `buildHashCommand` собирается из того же `commandPrefix`.
  - Предел оставлен прежним, 32 KiB, — вчетверо ниже замеренного потолка; на встраиваемых
    системах он ниже обычного, и запас взят под них. Подъём к 128 KiB обсуждался и отклонён
    владельцем: на обычных именах первой срабатывает граница в сотню имён (32 KiB на сотню —
    это 327 байт на имя, вчетверо больше обычного пути), поэтому команд меньше не станет,
    а перебор длины приходит пустым выводом, который читается как расхождение.
  - Юниты 856 → 864 (восемь новых). Обе границы под своим тестом: по числу имён (202 имени
    дают ровно три команды) и по длине. Отдельно: кириллица даёт больше команд, чем латиница
    той же длины; апостроф считается по экранированной длине; под sudo дробление мельче;
    начало команды занимает место наравне с именами; в отправленном ровно те имена, что
    просили, и каждое по одному разу.
  - Живые 162 → 168, новый файл `tests/live/verify-command-length.live.test.ts`: дерево из
    300 файлов с тяжёлыми именами уезжает и сверяется целиком, скачивается обратно и
    сверяется снова, а список действительно уходит несколькими командами — это считается
    обёрткой над настоящим исполнителем, а не выводится из ответа.
  - Негативный контроль: снятое дробление (оба предела подняты) роняет живой тест на обоих
    серверах; символьный счёт вместо байтов роняет пять юнитов; забытый sudo — два.
    Откат копией файла.
  - **Достижимость дефекта, замеренная, а не предполагаемая.** Без sudo счёт в знаках вместе
    с пределом 32 KiB держал команду ниже потолка даже на худших именах (123 702 из 131 072).
    Под sudo дефект уверенный, но рекурсивная заливка под sudo сегодня отвергается на входе
    (`transfer-tool.ts:584`), а одиночный файл в предел влезает всегда. Значит починен учёт,
    который был неверен, а не отказ, который наблюдался бы сегодня.
  - Мутанты на границах `splitIntoCommands` оставлены сознательно; пересчитано при работе
    над 3.6, потому что первая запись была неточной. Живут трое: `>` → `>=` в проверке
    длины, `current.length > 0` в двух местах и знак у обвязки sudo (`+` → `-`, проверено
    руками — выживает). Первые сдвигают границу на одно имя, последний под sudo даёт
    команду на тридцать байт длиннее при пределе в 32 KiB. Тест на каждого потребовал бы
    подгонки длин под точное равенство, а данным ни один не грозит.
- [x] **3.6. Названный таймаут стоял на каждой команде сверки, а не на всей операции.** ✅
  - **Правка** (`remote-verify.ts`): `startDeadline` заводит часы на входе в
    `verifyRemoteFiles` — до пробы паспорта, потому что она тоже идёт на сервер и тоже
    тратит обещанное. Перед каждой командой считается остаток и уезжает вместо полного
    срока. Ноль или отсутствие срока означает «потолка нет», и тогда часов не заводим вовсе.
  - Исчерпание даёт новый исход `deadline` в семье «проверить нечем» рядом с `truncated` и
    `guard-killed`. Все три собраны в `IncompleteAnswer`, и обе ветки — первый сбор и
    повторный после `tool-missing` — разбирают их одной проверкой: расхождение здесь
    недопустимо, по нему установщик сносит уже уехавшее дерево.
  - **Дефект, которого в плане не было, найден живым сценарием.** На исходе срока команде
    достаются последние миллисекунды, локальный сторож её убивает, и раннер бросает
    `SSHTimeoutError`. Замерено на обоих контейнерах: сверка не возвращала исход вовсе —
    исключение улетало в `transfer-tool`, где читается как провал передачи. Теперь
    `askWithin` ловит ошибку и, если срок к тому моменту исчерпан, отвечает «проверить
    нечем»; ошибка не про время идёт наверх как прежде.
  - Юниты 864 → 875 (одиннадцать новых). Оба цикла под своим тестом — и куски, и трудные
    имена по одному. Отдельно: остаток убывает от команды к команде; исчерпание даёт
    `unavailable`, хотя собранные хэши сошлись, а недостающие выглядели бы расхождением;
    остаток, съеденный ровно в ноль, — уже исчерпанный срок; проба паспорта тратит тот же
    срок; повторный сбор идёт с остатком, а не с новым сроком, и его собственное
    исчерпание тоже читается как «нечем»; ошибка не по времени пробрасывается; без срока
    каждая команда получает ноль. Три причины «нечем» названы каждая своим текстом.
  - Живые 168 → 174, новый файл `tests/live/verify-deadline.live.test.ts`: на дереве из
    250 файлов честного срока хватает и каждой команде достаётся строго меньший остаток;
    заведомо малый срок отвечает «проверить нечем», а дерево на сервере остаётся целым;
    следующая сверка тем же списком проходит обычным порядком — убитая по сроку команда
    не портит соединение.
  - Негативный контроль: возврат срока на каждую команду роняет пять юнитов и оба живых
    утверждения на обоих серверах (`SSHTimeoutError` вместо исхода — тот самый дефект);
    граница `> 0` → `>= 0` роняет два юнита; часы после пробы паспорта — один; выпадение
    `deadline` из «нечем» — четыре; проглоченная чужая ошибка и всегда проброшенная —
    по одному. Откат копией файла.

---

## Чанк 4 — хвосты

- [x] **4.1. Роутерный узел без sftp.** — *сделано 2026-08-06*
  - **Замер подтвердил ожидание.** На узле 2233 валятся все три формы передачи — загрузка,
    скачивание, рекурсивная загрузка: `sh: /usr/lib/ssh/sftp-server: not found` и следом
    `scp: Connection closed`, код 255. Через раннер это `SSHTransportError`. После отказа
    на сервере не остаётся ничего — частичного файла нет.
  - **Ломается только передача.** Мультиплексирование к dropbear работает (master
    поднимается, `-O check` отвечает, вторая команда идёт 8 мс), паспорт снимается штатной
    пробой: `bash=0 sha256=sha256sum coreutils=busybox timeout=1 install=1 home=/root`.
    `sudo`, `bash`, `openssl`, `rsync` на узле нет, `pwuser` контейнер не заводит — парольная
    ветка на нём непроверяема.
  - **`scp -O` чинит целиком** на роутере и ничего не ломает на двух других узлах.
  - **Узнать про отсутствие sftp заранее нечем — замерено, а не предположено.** `ssh -s sftp`
    даёт одинаковый код возврата на всех трёх узлах, отличается только текст; наличие бинаря
    `sftp-server` врёт в обе стороны (на 2231 стоит `internal-sftp`, а при живом бинаре
    подсистему выключают конфигом). Честная проба означала бы кусок протокола sftp в нашем
    коде ради вопроса, на который отвечает сама передача.
  - **Сделано: откат по факту неудачи.** `buildScpArgs` принимает `legacyProtocol` — ставит
    `-O` (только модерному клиенту, до 9.0 флага нет) и переводит путь в режим `shell`.
    `transfer()` при неудаче делает одну повторную попытку и запоминает назначение в
    `legacyScp`; вторая неудача возвращает **исходную** ошибку. Отмена и исчерпание срока
    повтора не вызывают. Откат либо чинит, либо не меняет ответа.
  - **Семейство имён проверено сразу, а не по одному.** Одиннадцать имён живьём на роутере
    и пятнадцать в замере на трёх узлах: пробел, апостроф, двойная кавычка, `$( )`,
    обратная кавычка, `;`, `&&`, `>`, `*`, обратный слэш, кириллица. Все лежат буквально,
    содержимое сходится в обе стороны, **ни одного маркера подстановки**. Перевод строки
    отклоняется нашим кодом. Дописывать в `prepareRemotePath` нечего.
  - `routerUnavailableReason` в `lab.ts` — свой сторож: роутер вне `LAB_SERVERS`, и без него
    «контейнер не поднят» читалось бы как «проверка прошла».
  - **Мутации нашли два недописанных теста в свежем коде:** транспортный отказ передачи был
    неотличим от неудачи самой передачи, а счётчик `transfersThisSession` считал откат за две
    передачи вместо одной (счёт переехал из попытки в передачу). Оба под тестом.
  - Тесты: юниты 875 → **893** (+6 в `ssh-args.test.ts`, +12 в `openssh-runner.test.ts`);
    в моке раннера появилось обязательное поле `scpOverSftp`, которого там не было вовсе.
    Живые 174 → **181**: новый файл `tests/live/router-legacy-scp.live.test.ts`, 7 утверждений
    на третьем узле. `tsc` чист, живая сетка идёт 58 секунд.
  - Негативный контроль по обоим местам: снятое условие отката роняет 6 живых из 7, снятый
    флаг `-O` — 5 из 7 (зелёными остаются проверка команд и отказ на переводе строки, он
    срабатывает до запуска `scp`). Мутация класса ошибки и мутация счётчика роняют каждая
    свой новый юнит. Откат — копией файла.
  - **Две ошибки замера поймал контрольный узел, а не рассуждение.** Сначала в спецификации
    `scp` не было пользователя — «ломалось» одинаково везде. Потом три имени с подстановкой
    содержали слэши внутри, то есть были путём через несуществующие каталоги; честное
    `No such file or directory` я принял за дефект классического протокола.
- [x] **4.2. Кэш раннеров без предела** — *снято: замер не подтвердил цену (2026-08-07)*
  - **Замер** (5001 назначение, объекты транспорта без соединений): **0.46 КБ на запись**,
    пять тысяч записей — 2.26 МБ. Вытеснения нет вовсе: первый объект жив после всех
    последующих.
  - **Роста от запросов не бывает.** Ключ кэша — `пользователь@хост:порт`, и координаты
    приходят только из файла профилей: ни один инструмент не принимает `host` или `port`
    аргументом. Значит записей ровно столько, сколько различных назначений встретилось за
    жизнь процесса, — предел на память решал бы задачу, которой нет.
  - **Настоящая цена у соседнего дефекта:** запись держит **секрет удалённого профиля до
    конца процесса**, и рядом лежит второй кэш тем же ключом и с тем же свойством —
    `passportCache` (`passport.ts:148`). Это тот же класс, что 4.7 (устаревшее состояние
    переживает перечитывание профилей), поэтому лечится там, а не пределом здесь.
- [x] **4.3. Ворота первой команды** — *сделано 2026-08-07*
  - **Замер до правки, по логу sshd в контейнере** (залп из пяти команд): первый холодный
    старт даёт **1** вход — шлюз работает; **второй холодный старт на том же транспорте
    даёт 5**; залп с неверным паролем — **5** отказов подряд. Шлюз закрывался однажды за
    жизнь объекта, а профиль остывает каждый раз, когда соединение закрыли или оно истекло
    по сроку простоя.
  - **Сделано.** Шлюз стал многоразовым: он снимается в `finally`, а решение «идти сразу»
    принимает новая отметка `masterSeenAt` — время, когда команда в последний раз доказала,
    что master поднят. Отметку снимает `closeMaster()`, и она устаревает сама через
    `ControlPersist`. Спрашивать `ssh -O check` перед каждой командой не стали: это лишний
    процесс на вызов, а срок простоя — величина известная.
  - **Ждавшие возвращаются к началу, а не идут выполнять.** Иначе после провала первой
    команды все ожидавшие уходили залпом, и сервер видел пять неудачных входов вместо
    одного — ровно то, за что срабатывают счётчики неудачных попыток.
  - Тесты: +4 юнита в `openssh-runner.test.ts` (901 → **905**) и новый живой файл
    `tests/live/first-command-gate.live.test.ts`, 6 утверждений на двух серверах
    (193 → **199**). Живой счёт берётся из лога sshd, а не из наших вызовов: залп и
    очередь дают одинаковое число вызовов и разное число входов. Юниты считают
    наибольшее число одновременных запусков — та же величина с другой стороны мока.
  - Пользователь `gateuser` заводится тестом и удаляется после: под `root` и `deploy` ходят
    соседние живые наборы, и их входы попали бы в тот же счёт. На Alpine пришлось повторить
    приём лаборатории — `adduser -D` оставляет `!` в shadow, то есть запертую учётку, и вход
    по ключу не проходит.
  - Негативный контроль четырьмя мутациями: снятый сброс шлюза — 60 юнитов (без него
    ожидание зацикливается); неснятая отметка при закрытии — 1 юнит и **оба живых
    утверждения про второй холодный залп** (`expected 5 to be 1`); снятая простановка
    отметки — 1 юнит (команды при живом соединении начинают ждать друг друга); возврат
    ждавших сразу в исполнение — 1 юнит. Откат копией файла.
- [x] **4.4. Запись askpass не атомарна** — *сделано (2026-08-07)*
  - Замер на настоящем коде: писатель 20 000 раз пересоздавал скрипт, читатель параллельно
    выполнял его как это делает ssh. **56 запусков из 330 напечатали пустоту** вместо секрета —
    для клиента это пустой пароль и отказ во входе. Окно берётся из того, что запись в
    конечный путь сначала обнуляет файл, а управляющий каталог общий: и для всех профилей
    процесса, и для соседних процессов сервера.
  - Правка одна: скрипт пишется под именем `askpass.sh.<pid>` рядом и встаёт на место
    переименованием (`askpass.ts:43-57`). Тот же замер после правки — 712 запусков, ни одного
    пустого. Счётчика во временном имени нет: внутри процесса запись синхронна, различать
    нужно только процессы.
  - Тесты: +3 юнита в `askpass-runtime.test.ts` (905 → **908**) и живое утверждение в
    `password-auth.live.test.ts` (199 → **201**). Живое проверяет вход по паролю на 24 попытки,
    пока сосед переписывает скрипт; мультиплексирование там снято намеренно — через готовое
    соединение askpass не читается вовсе, и залп прошёл бы даже по обнулённому файлу.
  - Негативный контроль тремя мутациями: запись обратно в конечный путь — 1 юнит и **оба живых
    утверждения** (`expected [ 255, 255, 255 ] to deeply equal []`); снятое переименование —
    6 юнитов; снятый `chmod` на временном файле — сначала **не поймал никто**, поэтому дописан
    юнит на уцелевший от падения временный файл с чужими правами. Откат копией файла.
- [x] **4.5. Откат при исчерпании сессий есть только у команд** — *сделано иначе, чем
  задумывалось (2026-08-07)*
  - Замер опроверг постановку. Занял все 10 сессий управляющего соединения (`MaxSessions 10`
    в лаборатории) и постучался: клиент печатает жалобу и **сам открывает отдельное
    соединение**, возвращая нулевой код. Так ведут себя и `ssh`, и `scp` обоими протоколами.
    Проверено тремя клиентами: 10.2 локально, 9.7 в alpine, 9.2 в debian — последние два
    замером изнутри контейнера на собственный sshd. Переносить в `transfer()` нечего:
    отката не существует, ветка в `exec()` не срабатывала ни разу.
  - Зато вскрылось настоящее: жалобу клиент печатает в stderr, и она приезжала вызывающему
    как вывод команды — `mux_client_request_session: ...` плюс `ControlSocket ... already
    exists, disabling multiplexing`. Кто судит об успехе по непустому stderr, видел ошибку
    там, где её не было.
  - Правки: снята мёртвая ветка отката в `exec` (`openssh-runner.ts:104-117`); служебные
    строки мультиплексирования убираются из stderr результата (`stripMuxNotices` в
    `error-classifier.ts:67-85`), классификатору при этом достаётся нетронутый вывод;
    разбор шаблонов mux опущен ниже транспортных (`:165-174`) — до кода 255 дело доходит
    только когда клиенту не удалось и отдельное соединение, и называть причиной лимит
    сессий значило бы подменить диагноз.
  - Тесты: +2 юнита в `openssh-runner.test.ts` (переписан прежний тест отката), +4 в
    `error-classifier.test.ts` (908 → **913**), новый живой файл
    `tests/live/mux-limit.live.test.ts` — 4 утверждения на двух серверах (201 → **205**).
    Живой держит сессии голыми `ssh` на том же сокете и требует, чтобы хотя бы один
    держатель получил отказ: иначе предел не выбран и проверять было бы нечего.
  - Негативный контроль тремя мутациями: сырой stderr в результате — 2 юнита и **оба живых
    утверждения про чистый ответ**; шаблоны mux обратно выше транспортных — 1 юнит;
    снятый шаблон про `ControlSocket` — 1 юнит. Откат копией файла.
  - Ловушка уборки: `pkill -f "sleep 45"` совпадает с собственной командной строкой и
    убивает свою же сессию раньше, чем дело дойдёт до `rm`. В тесте шаблон записан как
    `sleep 4[5]`.
- [x] **4.6. Отпечаток конфигурации склеен пробелом** — *снято: прочитано неверно (2026-08-05)*
  - `configFingerprint` склеивает поля не пробелом, а нулевым байтом, и коллизии на путях с
    пробелами не бывает. В исходнике стоит настоящий байт `\0` внутри кавычек, поэтому при
    беглом чтении он выглядит пустым местом. Задача снимается.
  - **Зато вскрылось другое: из-за этого байта `grep` считает файл двоичным и молча ничего
    в нём не находит.** На это наступили и я, и два агента: поиск по `openssh-runner.ts`
    возвращал пустоту, которую легко принять за «такого кода нет». Из-за этого я записал
    в долг неправду — будто частичный вывод убитой команды заполняет только `ssh2`;
    на деле системный клиент делает это тоже. Обе записи исправлены.
  - **Сделано (2026-08-05).** Байты заменены на escape-последовательность в
    `openssh-runner.ts` и `tests/unit/transfer-runner.test.ts` — значение то же, файлы
    снова текстовые. Заведён сторож `tests/unit/source-hygiene.test.ts`: проходит по
    отслеживаемым исходникам и падает на управляющих байтах, называя файл, строку и код
    знака. До починки он краснел ровно на этих двух местах.
  - Класс ошибки не ловил ни один из трёх сторожей проекта: компилятор видит корректный
    литерал, мутации проверяют логику, CI гоняет то и другое. Байт попадает в файл не по
    замыслу, а на границе передачи — в коде пишется литерал из двух знаков, а доезжает
    один байт, поэтому правилом такое не лечится, только сторожем.
- [x] **4.7. Производное состояние переживает перечитывание профилей** — *сделано (2026-08-07)*
  - Замер живьём на самом заметном из трёх: паспорт снят, утилита `timeout` убрана с сервера
    мимо нашего кода, профили перечитаны — команда вернула **код 127 и «timeout: command not
    found»**, потому что паспорт остался прежним и по-прежнему обещал обёртку. После правки та
    же последовательность даёт код 0.
  - Правка одна и в одном месте: `forgetDerivedState` (`profile-resolver.ts:143-160`) забывает
    секреты для маскировки, кэш транспортов и кэш паспортов. Соединения не закрываются —
    управляющий сокет общий для машины, и следующая команда садится на него же.
  - Зовётся только там, где файл действительно мог измениться. При истечении срока кэша
    (минута по умолчанию) — нет: иначе паспорт пересниматься раз в минуту, а это лишнее
    обращение к серверу на ровном месте.
  - Наблюдение за файлом теперь ходит через `reloadProfiles`, а не собирает перезагрузку
    само (`:187-199`). Было две дороги к одному действию, и вторая осталась бы без сторожа:
    в тестах наблюдатель выключен, и мутация в ней не покраснела бы ни от чего.
  - Тесты: новый юнит-файл `tests/unit/profiles-reload-state.test.ts` — 4 утверждения, по
    одному на каждое хранилище плюс пара «секрет нового файла маскируется» (913 → **917**);
    новый живой `tests/live/profiles-reload.live.test.ts` — 4 утверждения на двух серверах
    (205 → **209**). Живой держит негативный контроль внутри себя: до перечитывания та же
    команда обязана упасть с 127, иначе сцена не собралась и проверять нечего.
  - Негативный контроль тремя мутациями: снятый `forgetLoggedSecrets` — 1 юнит; снятый
    `resetRunnerCache` — 1 юнит; снятый `resetPassportCache` — 1 юнит и **оба живых
    утверждения** (`expected 127 to be +0`). Откат копией файла.
  - Следов не осталось: `timeout` на месте на обоих контейнерах, спрятанной копии нет.
- [x] **4.8. Сквозной тест есть не у всех полей профиля.** — *сделано 2026-08-07*
  - **Дефекта в цепочке нет — это сторож, а не починка.** Все четыре поля доезжают:
    замер на обоих контейнерах показал нужный контейнер, нужного пользователя и вход
    каждым из трёх способов. Правок в `src` задача не содержит.
  - Живой файл `tests/live/profile-fields.live.test.ts`: у каждого поля своя пара —
    порт (профили различаются одним полем, и `/etc/os-release` называет разные
    контейнеры), ключ (чужой ключ не пускают), пароль (неверный не пускает), passphrase
    (ключ с фразой заводится тестом на время прогона, неверная фраза не пускает).
    Утверждается состояние сервера: команда пишет своё `id -un` в файл, а файл читается
    **отдельным** соединением мимо проверяемого кода.
  - **Первая версия живого файла была ложно зелёной, и показала это мутация.** Профиль
    без пароля вход не проходил, но проверка `ответ содержит "pwuser"` зеленела на самом
    отказе: в тексте отказа стоит `pwuser@127.0.0.1: Permission denied`. Мутации «убрать
    `password`» и «убрать `passphrase`» из сборки конфигурации не роняли ничего. После
    перехода на след на сервере обе роняют по два живых утверждения.
  - Второй сторож в том же файле: свой каталог управления сносится перед прогоном.
    Мастер, оставшийся от прошлого прогона, пустил бы команду с заведомо неверными
    данными — кэш у нового процесса пуст, и смены учётных данных он не увидит.
  - Юниты: цепочка «файл → загрузчик → конфигурация → аргументы команды» замкнута для
    порта (в `-p` и в `-P`, строкой в JSON, отсутствие поля → 22), ключа (`IdentityFile`
    плюс `IdentitiesOnly`, раскрытие тильды — живьём его не проверить, не насорив в
    домашнем каталоге) и обоих секретов (`SSH_MCP_SECRET`, снятый `BatchMode`).
    893 → **901**, живых 181 → **193**.
  - Негативный контроль семью мутациями: убрать из сборки конфигурации `port` — 6 живых
    и 3 юнита, `privateKeyPath` — 4 и 3, `password` — 2 и 1, `passphrase` — 2 и 1;
    жёсткий `-p 22` — 6 и 2. Ещё две названы и оставлены: снятый `IdentitiesOnly` держит
    только юнит (в агенте разработчика ключей нет, перебирать нечего), а первая ветка
    `selectSecret` (`askpass.ts:57-59`) эквивалентна последней строке — при заданной
    фразе `passphrase ?? password` отвечает тем же. Откат каждой — копией файла.
  - Следов не осталось: пробный ключ убран из `authorized_keys` обоих контейнеров, файл
    цел, маркеры и каталог управления сняты.
- [x] **4.9. `ssh_exec` не различает свои две ветки под тестом.** — *сделано 2026-08-07*
  - **Дефекта в коде нет — это сторож, а не починка.** Правок в `src` задача не содержит,
    закрыта форма ответа, которую читает агент.
  - Замер до работы подтвердил постановку целиком: по `exec-tool.ts` выживало **133 мутанта
    из 247** (балл 37.25). Среди них — та самая развилка `commands.length === 1`, обе строки
    `sudo: args.sudo || false`, вся схема инструмента (имя, `required`, `oneOf` у `command`,
    типы полей) и все двенадцать образцов опасных команд.
  - Новый юнит-файл `tests/unit/exec-response-shape.test.ts`: объявление инструмента; форма
    одиночного ответа (голый вывод, «no output», нулевой код не приписывается, `STDERR:`
    после вывода); форма пачки — **разбор сверяется целиком, знак в знак**, потому что по
    кускам отступы и переводы строк не проверяет никто, а именно они отделяют вывод одной
    команды от другой; опции в обеих ветках; предупреждения по каждому образцу.
  - В `tests/unit/exec-destructive-guard.test.ts` добавлены три пары к уже бывшим: дом
    снимается один раз на весь батч, ссылка «туда же, где названа» удаление не отменяет,
    резолв идёт с правами вызова. Мок стал злее: `readlink` отвечает разной целью для
    разных путей, а не корнем на любой вопрос.
  - Живой файл `tests/live/exec-response-shape.live.test.ts` — 7 утверждений на двух
    серверах. Юниты собирают ответ вокруг придуманного результата исполнителя, поэтому до
    этой границы форму задаёт сам тест; замер снял ту же форму с настоящего сервера и
    расхождений не показал. Наборы утилит расходятся текстом и кодом ошибки `ls` (1 у
    BusyBox, 2 у coreutils) — разбор ответа не расходится.
  - Итог: 133 → **31 выживший**, балл 37.25 → 87.45. Юниты 917 → **972**, живые 209 → **223**.
  - Остаток осмотрен и оставлен намеренно: тринадцать мутантов `\s+ → \s` в регулярках
    (на одном пробеле поведение то же), обнуление `description` в схеме и текста в журнале,
    `if (result.stdout) → if (true)` (пустая строка добавляется как ничто) и резолв пустого
    списка целей, который на сервер не ходит. Сверять текст описания дословно — значит
    переписать код в тест.
  - Мутации живого файла Stryker не гоняет, поэтому негативный контроль вручную:
    `if (commands.length === 1) → if (false)` роняет **10 из 14** живых утверждений. Откат
    копией файла.
  - **Два теста написаны и сняты.** Проверки, что `profileName` доезжает в опциях вызова,
    закрепляли бы цепочку, которую [TD-10](../../tech-debt/profile-name-flows-nowhere_10.md)
    предписывает снять: поле передаётся тремя слоями и не читается никем. Сторож на мёртвом
    поле удорожает будущую уборку и обещает читателю смысл, которого нет.
  - Следов на контейнерах нет: живой файл выполняет только `echo`, `true`, `ls`, `id`, `pwd`
    и сносит свой каталог управления.
- [x] **4.10. Обвязка запуска процесса под тестом почти не различима.** — *сделано 2026-08-07*
  - **Дефекта в коде нет — это сторож, а не починка.** Правок в `src` задача не содержит.
    Замер подтвердил постановку: 31 выживший из 101, балл 69.31.
  - Половина названного в пункте снаружи не наблюдается вовсе, и это выяснилось замером, а не
    рассуждением. Нужны были инструменты, которых у результата нет: `process.getActiveResourcesInfo()`
    считает таймеры, удерживающие цикл событий, `getEventListeners` из `events` — слушателей на
    сигнале отмены. Ими и закрыто главное: **срок команды снимается** (иначе быстрая команда с
    часовым сроком держала бы сервер) и **подписка на отмену снимается** (иначе на общем сигнале
    слушатели копятся от запуска к запуску). Третий тест гоняет три запуска на одном сигнале —
    утечка видна ростом, а не одиночным числом.
  - Найдено по дороге и закрыто тестом: запись в ввод, который команда уже закрыла, приходит
    отдельным событием потока, и без своего обработчика она уносит **весь процесс сервера**, а не
    одну команду. Мутация «снять обработчик» валит прогон целиком (`write EPIPE`, код возврата 1) —
    Stryker считает такое ошибкой, а не убийством, но сторож работает.
  - Ответ на непроизошедший запуск (сигнал был отменён до старта) сверяется целиком: раньше
    проверялись два поля из восьми, и «обрезано», «убито по сроку», «код 3» на пустом месте
    прошли бы незамеченными. Срок работы получил верхнюю границу — без неё сходила и сумма двух
    отметок времени вместо разности.
  - Вход в накопитель закрыт сценой, которой не было: порция **ровно в лимит** сама по себе целая,
    и обрезкой становится только следующая. Мутанты именно этих трёх строк эквивалентны — пометка
    переезжает в соседнюю ветку и результат тот же, — но тест не пустой: мутация «войти и промолчать»
    роняет его.
  - Итог: 31 → **22 выживших** (плюс один, на котором прогон падает), балл 69.31 → 78.00.
    Юниты 972 → **977**, живые 223 без изменений.
  - Остаток осмотрен: `settled` — вторая линия обороны за снятыми таймером и слушателем, её путь
    достижим только вместе с поломкой первой, то есть парной мутацией; `once: true` на сигнале,
    который по устройству стреляет один раз; `killTimer.unref?.()` (метод есть всегда) и его
    `clearTimeout` — таймер откреплён от цикла событий и потому невидим; `?.` на потоках, которые
    при `stdio` по умолчанию есть всегда; условие срока в трёх видах — все совпадают на
    единственных приходящих значениях; `truncated` в ветке неудачного запуска — вывода там нет
    вовсе, обрезать нечего.
- [x] **4.11. Четвёртая запись правила защиты имени осталась в поиске по логам.** — *сделано 2026-08-07*
  - Найдено при сведении в задаче 3.3: `escapeQuery` делал ту же подстановку апострофа, но без
    обрамления — кавычки дописывались в шаблоне команды. Обе точки сведены к `shellQuote(query)`,
    четвёртая запись правила снята. Замер до и после совпал байт-в-байт: поведение то же, копий
    правила меньше.
  - **Проба из пункта оказалась слаба, и это выяснилось замером.** Строка `$(…)` не находилась не
    из-за защиты, а из-за regex-семантики `grep -E`: `$` там значит конец строки. Такая проба
    прошла бы и на дырявом коде. Заменена на `x' ; touch МАРКЕР ; echo 'y` — она одновременно
    готовая команда для shell и обычный образец для `grep`, поэтому доказательство двойное:
    **нашлась** (значит доехала целиком) и **маркер не появился** (значит не выполнилась). Проверка
    только на маркер прошла бы и там, где строка не доехала вовсе.
  - Сторож живой, в `tests/live/shell-quoting.live.test.ts` — файл ровно про это правило. Три пробы
    на каждом сервере: одиночный журнал, под sudo (кавычки накладываются дважды) и пачка.
  - Мутационный контроль поймал недоделку в самом тесте. Маркер сперва был один на весь блок:
    снятие кавычек в одиночной ветке валило и пачечный тест — не своим дефектом, а чужим следом.
    То есть сторожа у второй точки не было. После разведения маркеров по пробам мутации бьют
    точно: снять защиту в одиночной ветке — падают два теста на сервер, в пачечной — один.
  - Обе половины утверждения проверены на способность краснеть по своей причине: при снятой защите
    команда распадается (`grep 'x'`, а `; touch … ; echo 'y…'` уходит отдельной командой) и тест
    падает на «не нашлось»; при заранее созданном маркере — на «YES вместо NO».
  - Итог: живые 223 → **229**, юниты 977 без изменений, `tsc` чист. Следов на контейнерах нет —
    файл сносит свои каталоги и маркеры в `afterAll`.
  - Stryker по `log-tools.ts` показывает 96 выживших из 210 (было 97 — ушло тело снятого метода),
    и это ожидаемо: юнитов у файла нет вовсе, 94 строки идут как «не покрыто». Мутации гоняют
    только юниты, поэтому сторож здесь проверен вручную живой мутацией — по каждой точке вызова
    отдельно. Балл файла закрывается задачей 4.12, а не этой.
- [x] **4.12. Слой инструментов почти не различим под тестами.** — *сделано (2026-08-08)* Замер мутациями на границе
  задачи 3.3, по файлам: `audit-tool` 403 выживших, `transfer-tool` 162, `file-tools` 110,
  `log-tools` 97. Столько же строк вообще не покрыто (265 / 131 / 175 / 94). Инструменты —
  единственное, что видит агент, и именно они собирают строку команды. Задача не «поднять
  балл», а закрыть тестами разбор ответа сервера и сборку команды в каждом из четырёх.
  Файлы закрываются по одному; свежий замер перед началом подтвердил цифры: `audit-tool` 403,
  `transfer-tool` 158, `file-tools` 110.
  - [x] **`audit-tool` — сделано 2026-08-08.** 403 → **150 выживших**, балл 29.61 → 75.13
    (по покрытому коду 82.62), не покрыто 265 → 86. Правок в `src` нет.
    - Мутанты дважды показали, что **мок добрее сервера**, и оба раза правились тесты.
      Первый прогон оставил живой всю обрезку значений: подавался вылизанный текст, тогда как
      `uptime` приходит с отступом слева, вывод — с переводом строки в конце, а `netstat`
      выравнивает колонки несколькими пробелами. Списков из двух элементов не было нигде —
      именно на них ломается склейка через запятую.
    - Главное исправление — в самом моке: он отвечал заготовкой независимо от того, что уехало
      в команде, поэтому потерянный ключ раздела остался бы незамеченным. Теперь мок читает
      команду и печатает только запрошенные разделы, как сервер. Шесть тестов сразу покраснели —
      они подавали раздел `sshd`, не спросив его; вызовы исправлены.
    - Печать сводки закрыта **сверкой текста целиком** (приём из 4.9): по кускам отступы, пустые
      строки между разделами и ширину колонок не проверяет никто. Сверок две — спокойный сервер
      и тревожный, где печатаются блоки предупреждений, «проверить нечем» и живой docker.
      Выжившие в печати: 14 → 5.
    - Живой файл закрепляет инвариант честного ответа: пустой список слушателей либо объяснён,
      либо настоящий, третьего исхода нет. Замер на контейнерах показал оба: alpine отвечает
      через `netstat` с выравниванием, debian не имеет ни `ss`, ни `netstat` и получает пометку.
      Мутация «снять пометку» валит тест на debian.
    - Юниты 977 → **1120**, живые 229 → **241**.
    - Остаток осмотрен: `args.profile || 'default'` не трогаем намеренно — поле мёртвое и
      предписано к сносу в [TD-10](../../tech-debt/profile-name-flows-nowhere_10.md); вариации
      регулярных выражений (`\s*` → `\s`, снятые якоря) на настоящем выводе `openssl` и `sshd -T`
      неотличимы; часть строк формата команд закрывается только сверкой всей строки целиком,
      что для команды из пятнадцати кусков дало бы тест, ломающийся от любой правки.
  - [x] **`transfer-tool` — сделано 2026-08-08.** 158 → **19 выживших**, балл 67.15 → 95.69
    (по покрытому коду 96.08), не покрыто 131 → 2. Правок в `src` нет. Юниты 1120 → **1196**.
    - Сервер в тестах не заготовка: он ведёт своё дерево путей, применяет `mkdir`, `mv -T`,
      `rm -rf` и `install`, считает хэши от того, что в нём лежит, а команду, которой не ждал,
      **отклоняет**. Заготовка на любой запрос пропустила бы и потерянный кусок команды, и
      обращение не к тому пути — на аудите это был главный дефект самих тестов.
    - Мок оказался добрее сервера ровно там, где сервер строг: `chmod` отвечал успехом на
      любой аргумент, поэтому мутант «ставить права всегда, даже без `mode`» выживал —
      настоящий `chmod undefined` отказывает. После правки мутант умирает.
    - Закрыта запись под root целиком: стадирование в `/tmp`, флаги `install` по частям
      (`-m`, `-o`, `-g` из `owner` с группой и без), создание родителя под sudo, уборка
      промежуточного файла даже при провале установки, сверка установленного под sudo.
    - Три исхода сверки разведены и в тексте: сошлось, не сошлось (названы операция и все
      расходящиеся файлы), проверить нечем — с причиной. Сохранность проверена ответом
      сервера, а не словами: после расхождения боевой путь пуст, а прежний файл у человека
      цел; неубранная старая копия названа адресом и остаётся предупреждением, а не провалом.
    - Печать сверяется целиком, включая блок предупреждений: двух предупреждений — две
      строки, склейка их не теряет.
    - Остаток осмотрен. Эквивалентные: `.trim()` перед `Number` (пробелы тот и так глотает),
      `{ verified: false }` → `{}` (undefined печатается как «skipped»), `catch {}` вместо
      `catch { return false }` (оба ложны), начальное значение списка файлов, которое
      перезаписывается до чтения. Не проверяем намеренно: тексты в журнал (в ответе их нет)
      и куски составных описаний в схеме — сверка описаний целиком дала бы тест, ломающийся
      от любой правки текста. Отдельно: `atomic` для каталога никуда не влияет — каталог
      всегда встаёт на место переименованием, и параметр в этом маршруте мёртв.
  - [x] **`file-tools` — сделано 2026-08-08.** 110 → **28 выживших**, балл 35.96 → 93.48
    (по покрытому коду 93.69), не покрыто 175 → 1. Правок в `src` нет. Юниты 1196 → **1276**.
    - Сервер в тестах ведёт своё дерево путей тем же способом, что и у `transfer-tool`, и
      дополнительно умеет то, чем живут файловые инструменты: наполняет файл из потока на
      входе команды, читает его `cat` и `base64`, перечисляет каталог и сам раскрывает
      шаблон. Неизвестную команду отклоняет.
    - Закрыта сводка по пачке файлов — то единственное, что видит агент, когда файлов больше
      одного. Сверяется целиком: заголовок, размер каждой записи в байтах, разделитель,
      крест у неудачной записи с текстом от сервера. По кускам ни отступ, ни потерянный
      разделитель никто бы не заметил.
    - Разведены три исхода чтения, которые до этого сливались в один: содержимое, отказ
      сервера и обрезанный ответ. Обрезанное содержимое не выдаётся за файл, а ненулевой код
      остаётся ошибкой сервера, даже если ответ заодно обрезан.
    - Объявление инструментов проверяется не текстом, а формой: у каждого поля есть тип, у
      объявленного описания — непустое значение, у варианта «строка или список» — свой тип.
      Такой тест переживает правку формулировки и ловит схлопнувшееся поле.
    - Уборка за собой проверяется в своём временном корне: `TMPDIR` на время теста подменён,
      и после чтения и записи он обязан остаться пустым. По общему каталогу системы это
      проверять нельзя — там одновременно работают другие тесты.
    - Остаток осмотрен. Эквивалентные: пустая кодировка вместо `utf8` (Node считает так же),
      `content: ''` у неудачной записи (поле не печатается), `kind: 'file'` → `""` (установщик
      ветвится только на каталоге), `warnings || []` (поле всегда список), `force: false` при
      уборке существующего каталога, имя локального временного каталога. Не проверяем
      намеренно: тексты в журнал и куски составных описаний инструментов.
  - [x] **`log-tools` — сделано 2026-08-08.** 96 → **7 выживших**, балл 37.50 → 97.70,
    не покрыто 94 → 0. Правок в `src` нет. Юниты 1276 → **1326**.
    - Сервер в тестах держит журналы и сам исполняет по ним `tail` и `grep`: отсчитывает
      строки с конца, нумерует их, отбирает по выражению, добавляет соседние строки при
      запрошенном контексте и различает регистр по флагу. Заготовка ответила бы одинаково
      и на `-i`, и без него, и потерянный флаг остался бы незамеченным.
    - Сводка по нескольким журналам сверяется целиком: заголовок с числом строк или с самим
      запросом, раздел на журнал с числом строк и совпадений, крест у недоступного журнала.
      Пустой журнал считается нулём строк, а не одной пустой; журнал без совпадений подписан
      словами, а не выглядит пустым разделом.
    - Пометка об обрезке проверена в четырёх местах, где она печатается по-разному: у
      одиночного хвоста и поиска её ставит общий помощник, в сводке она приклеивается к
      своему разделу — и не должна расползаться на соседние.
    - Остаток: тексты в журнал (в ответе их нет) и поля неудачной записи (`lines: []`,
      `matches: ''`) — при неуспехе они не печатаются вовсе.

- [x] **Граница чанка 4: мутации по всему тронутому** 🟢 — *сделано (2026-08-08)*
  - Прогон по 44 файлам, которые ветка изменила относительно `main`: 2 минуты 36 секунд,
    убито 4492, выжило 1112, не покрыто 792. Общая цифра 70.29% ни о чём не говорит и целью
    не является — читается картина по файлам.
  - **Слой инструментов после 4.9 и 4.12:** `log-tools` 97.70 (7 выживших), `transfer-tool`
    96.10 (17), `file-tools` 93.48 (28), `exec-tool` 87.45 (31), `audit-tool` 75.13 (150).
    Остатки разобраны в своих задачах: журнал, эквивалентные и куски составных описаний.
  - **Два инструмента спринт не трогал**, и это видно: `snapshot-tool` 16.36 (134 выживших,
    137 строк без юнитов) и `monitoring-tool` 49.06 (44 и 64). Оба — в [TD-01](../../tech-debt/test-coverage_01.md),
    в этот спринт не берутся: их формат ответа не менялся, и работа там того же размера,
    что 4.12 целиком.
  - **Свежий код транспорта** держится на живых тестах, поэтому мутационный балл занижен по
    построению — мутации гоняют одни юниты: `openssh-runner` 62.26 (69), `runtime-check`
    54.26 (45), `askpass` 60.61 (13), `errors` 48.39 (16). Ключевые их места закрыты живой
    сеткой на трёх контейнерах и отдельными задачами чанка 3.
  - **Хорошо стоят** те файлы, где решение принимается без сервера: `removal-guard` и
    `tool-args` по 100, `get-runner` 100, `sha256` 96.55, `path-validator` 95.86,
    `output-notes` 95.35, `control-sockets` 94.74, `path-guard` 90.08.
  - **Слабое место вне инструментов — чтение профилей:** `ssh-config` 2.63 (72 строки без
    юнитов), `profile-resolver` 16.38 (89 и 105), `profiles-file` 40.12 (121 и 79). Файл
    профилей — вход, которому мы доверяем больше всего, а проверен он меньше всего.
    Записано в [TD-01](../../tech-debt/test-coverage_01.md); в этот спринт не берётся.
  - Двенадцать мутантов сняты по сторожу времени (зацикливание — то же убийство), один в
    `process.ts` не собрался.

---

## Чанк 5 — ревью ветки

Проводится **после сноса**: он перелопачивает половину тронутых файлов, и ревью, сделанное
раньше, частично протухнет. Код-ревью субагентом по накопленному диффу, `/security-review`
запускает владелец, мутации по всему тронутому.

---

## Долги, подтверждённые сверкой, но не взятые в спринт

Каждый проверен по коду. Записаны в `docs/tech-debt/` пунктом 1.3 — колонка «Где» ниже
исправлена по факту: два долга уже лежали в реестре, а часть ссылок вела не в тот каталог.

| Пункт | Где | Цена, если не трогать | Куда записан |
|---|---|---|---|
| Ни один ответ не несёт `isError` | все инструменты, кроме `monitoring-tool` | клиент не отличает провал от содержимого | уже был в TD-03 |
| Частичный вывод убитой команды не читает никто | `errors.ts:65-96` создаётся, в `src/tools/` не читается | вывод до обрыва теряется | уже был в TD-03 |
| Поиск следов не проверяет обрезку вывода | `installer.ts:257-271` → `managers/remote-path-ops.ts:112-123`, поле `truncated` не читается | назовём не все артефакты, молча | TD-06 |
| Гонка двух установок: проигравшему уезжает сырой текст `mv` | `installer.ts:163,168` | «cannot stat» вместо объяснения | TD-07 |
| Отмена не доходит до инструментов | `runner/types.ts:16,59` против `exec-tool.ts` и `transfer-tool.ts` | длинную операцию нечем прервать | TD-08 |
| `stat -c` без ветки для BSD и macOS | `managers/remote-path-ops.ts:72-82` | проверка точки монтирования молча «не знает» | TD-09 |

---

## Границы

- Имена инструментов и формат профилей не меняем — пакет опубликован в npm.
- Отмена уже запущенной команды и частичный вывод убитой команды — не сюда, это отдельная
  работа по SDK.
- Функциональность не сужаем: если что-то работает только на `ssh2`, это повод разобраться,
  а не повод удалить возможность.

## Проверка

- `npx tsc --noEmit` — чисто, включая мёртвые объявления.
- Юниты зелёные целиком; после чанка 2 назвать фактический счёт.
- Живая сетка на обоих контейнерах, а для задачи 4.1 — на всех трёх.
- Мутации по накопленному диффу на границе чанка: выживших в свежем коде нет, либо каждый
  назван с решением (тест / эквивалентный / в `docs/tech-debt/`). Итог читать по строке
  `Tests …`; её отсутствие означает, что прогон не состоялся.
- Живой тест `removal-guard.live` при выключенной врезке сносит лабораторию по-настоящему.
  После такой мутации: `docker rm -f mcp-alpine mcp-debian mcp-router && npm run lab:up`.
- Каждый новый живой тест проверен негативным контролем: сломать ожидание, убедиться, что
  краснеет, вернуть копией файла (не `git checkout` — им сносятся соседние правки).
