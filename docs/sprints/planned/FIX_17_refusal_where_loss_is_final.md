# FIX_17 — отказ там, где потерю уже не отменить

**Статус:** ✅ ЗАВЕРШЁН — шесть задач закрыты замерами на обоих контейнерах,
порог и его границы записаны в [ADR-007](../../decisions/007-refusal-threshold.md)
**Дата заведения:** 2026-08-16
**Ветка:** `feat/openssh-transport`
**Происхождение:** разбор после FIX_16 — защищена машина, а не данные инфраструктуры

---

## Зачем этот спринт

У инструмента две ступени осторожности. Первая — предупреждение: команда уходит на
сервер, в ответ добавляется `⚠️`. Вторая — отказ: ничего не отправляется, пока в команду
не дописан маркер `# CONFIRMED-DESTRUCTIVE`.

Сегодня на второй ступени стоит только рекурсивное удаление путей. Всё остальное, что
уносит данные навсегда, либо предупреждает, либо молчит вовсе. Замер на живой машине
(alpine, свежая сборка после FIX_16):

```
   тихо  docker compose down -v
   тихо  docker volume rm pgdata
   тихо  docker volume prune -f
   тихо  dropdb production
   тихо  rm -rf /var/backups/db
   тихо  mkfs.ext4 /dev/sdz
```

Шесть команд, каждая уносит данные без возврата, и ни одна не сказала ни слова.
`docker compose down -v` читается как «останови сервисы», а уносит тома с базой.

Отдельная беда — порядок работы. Правильный: сделать копию, перенести, потом удалить.
Ошибочный, и он частый: сначала удалить, потом обнаружить, что копировать уже нечего.
Такая цепочка видна в одном вызове целиком — значит её можно остановить до отправки, пока
данные ещё на месте.

---

## Что уже известно точно

| Факт | Где видно |
|---|---|
| Отказ умеет только удаление путей | `exec-tool.ts:401-433` — `refuseDestructive` зовёт `findRemovalTargets` |
| Отказ выносится до первой отправки, на весь вызов | `exec-tool.ts:210-219` |
| Предупреждения работают отдельно и после отказа | `exec-tool.ts:222-228` — `checkDangerousCommand` |
| Разбор команды на вызовы уже есть | `command-parse.ts:88` — `parseInvocations` даёт имя и аргументы по сегментам |
| Маркер и текст отказа готовы | `destructive-command.ts:22` и `:210` — `CONFIRMATION_MARKER`, `blockedMessage` |
| Остановка машины сейчас только предупреждает | `exec-tool.ts:36` — `HALTING_COMMANDS` в списке предупреждений |
| Запросы к БД видны только в тексте команды | `exec-tool.ts:79-82` — шаблон ищется, когда вызван клиент БД |
| У `refuseDestructive` ранняя ветка по удалению | `exec-tool.ts:412` — `if (findRemovalTargets(command).length === 0) continue` |

---

## Границы: чего в этом спринте не делаем

- **Имена инструментов и формат профилей не трогаем.** Пакет в npm.
- **Маркер один — `# CONFIRMED-DESTRUCTIVE`.** Второго не заводим: два написания придётся
  вспоминать, а вспоминают неправильно.
- **Обратимое остаётся предупреждением.** `chmod 777` ослабляет права и лечится обратной
  командой — отказ там мешает, а не защищает.
- **`git reset --hard` и `git clean` не берём.** Они меняют содержимое репозитория, сам
  репозиторий цел — это предупреждение по порогу.
- **`DROP TABLE`, `TRUNCATE`, `DELETE FROM` остаются предупреждением.** Таблица — содержимое
  базы, а не сосуд. Необратимость у них та же, что у `dropdb`, — решает уровень объекта.
- **Сторож удаления путей не переписываем.** У него своя проверенная тема — куда ведёт
  путь и что за ним стоит симлинк. Новый разбор живёт рядом, а не внутри.
- **Ответ «здесь менять нечего» — законный.** Не воспроизвелось на замере — задача
  закрывается замером.

---

## Порог: что попадает на вторую ступень

Отказ ставится по двум независимым проверкам. Сработала любая — отказ.

**Проверка 1 — что уничтожают.** Команда уничтожает **сосуд целиком**: машину, базу, том,
диск, набор заданий, системное дерево. Порядок в команде не важен.

**Проверка 2 — когда уничтожают.** Объект уничтожен, и дальше в той же команде он
**читается**. Что за объект — не важно, сосуд или обычный каталог.

Общий инвариант, из которого выведены обе:

> Уничтожение объекта — последняя операция над ним. После неё объект можно создать
> заново, но нельзя прочитать.

**Почему мерим уровень объекта, а не частоту команды.** Порог обязан отсекать штатную
работу: маркер защищает, только пока он редкость — предупреждение, звучащее каждый день,
перестают читать (так из списка ушёл `rm`, `exec-tool.ts:28-34`), а отказ начинают
обходить не думая. Но «часто ли встречается» — вопрос вкусовой, о нём спорят. «Сосуд или
содержимое» — вопрос проверяемый, и он даёт тот же отбор: сосуды не сносят по расписанию.

| Сосуд целиком → отказ | Содержимое → предупреждение |
|---|---|
| `reboot` — машина | `chmod 777` — файл |
| `docker compose down -v`, `volume rm/prune` — том | `docker rm -f` — контейнер, том цел |
| `dropdb`, `FLUSHALL` — база | `DROP TABLE`, `TRUNCATE`, `DELETE` — таблица внутри базы |
| `mkfs`, `wipefs -a`, `lvremove`, `zfs destroy` — диск/том | `git reset --hard` — файлы внутри репозитория |
| `crontab -r` — весь набор заданий | `crontab -e` — правка внутри набора |
| `rm -rf /`, `/etc` — дерево целиком | `rm -rf /var/log/old` — путь внутри дерева |

Проверки покрывают слепые зоны друг друга. `docker compose down -v` уносит тома, которых в
тексте команды нет вовсе — проверка 2 их не видит, срабатывает проверка 1. Наоборот,
`rm -rf A && cp -r A A.bak` не трогает ни одного сосуда — молчит проверка 1, срабатывает 2.

---

## Общая процедура каждой задачи

1. **Замер до кода.** Воспроизвести на стенде, записать ответ дословно.
2. **Правка** в названной точке.
3. **Замер после.** Тот же вызов, тот же стенд.
4. **Пара тестов, а не один.** Правило №4 проекта: у каждой команды проверяется и отказ,
   и молчание на соседней штатной форме. Сторож, который отказывает всегда, бесполезен
   ровно так же, как молчащий.
5. **Замер маркера.** Та же команда с `# CONFIRMED-DESTRUCTIVE` обязана пройти.
6. **`npm run mutate -- HEAD`** по тронутому, разбор выживших.
7. **Отчёт задачи — diff и вывод команд**, не слово «сделано».

### Про стенды

Отказ выносится **до отправки**, поэтому проверяется на любом контейнере лаборатории:
наличие `docker`, `lvm` или `psql` на нём не требуется — команда просто не уедет.

**Боевые серверы в проверках не участвуют.** Весь спринт меряется на лаборатории: и отказы,
и молчание на штатных формах. Отказ выносится до отправки, поэтому боевая машина не даёт
ничего сверх контейнера — а замер остановки машины стенд действительно кладёт.

Придумывать вывод команд руками нельзя — правило №6 проекта.

---

## Архитектура: где живёт новый разбор

Заводится отдельный модуль — назовём `src/utils/irreversible-command.ts`. Он отвечает на
один вопрос: «уносит ли эта команда данные навсегда» — по имени и аргументам, без
обращений к серверу. Чистая функция, как `destructive-command.ts`, и с тем же ответом:
заблокировано или нет, и почему.

```
command-parse.ts        →  что команда запускает (имя + аргументы)
   ├── destructive-command.ts    →  куда ведёт путь удаления (нужен сервер для симлинков)
   └── irreversible-command.ts   →  уносит ли команда данные (только текст)

exec-tool.ts: refuseDestructive() зовёт оба разбора до первой отправки
```

Почему отдельный модуль, а не список внутри `destructive-command.ts`: там речь про пути и
их резолв на сервере, здесь — про имена команд и флаги. Смешивать — получить модуль,
который делает две разные вещи и называется по одной из них.

**Новый разбор зовётся первым в `refuseDestructive`, до ранней ветки по
`findRemovalTargets`.** Иначе `reboot` не даёт целей удаления, срабатывает `continue`, и
проверка не выполняется вовсе — задача 1 провалилась бы молча. Сервер новому разбору не
нужен, паспорт остаётся ленивым.

**Перенесённое убирается из предупреждений.** `HALTING_COMMANDS`, `docker system prune -a`,
`DROP DATABASE` сейчас живут в `checkDangerousCommand`; оставить их там — значит на команде
с маркером выдать ещё и `⚠️` вдогонку. Тесты предупреждений правятся тем же заходом.

---

## 📊 ЗАДАЧИ

Порядок: сначала то, что чаще уносит данные, потом редкое, потом самое сложное.

### 1. Остановка машины требует подтверждения

**Что не так.** `reboot` только предупреждает — а предупреждение читают уже после того,
как команда ушла. Перезагрузка необратима и рвёт тот самый канал, по которому агент
работает: ответ он может не получить вовсе.

**Точка правки.** `exec-tool.ts:36` (`HALTING_COMMANDS` уезжает из предупреждений в новый
модуль) и `exec-tool.ts:401` (`refuseDestructive`).

**Что должно стать.** `reboot`, `shutdown`, `halt`, `poweroff` — отказ с маркером.
Обёртки (`sudo`, `timeout 5`, `nice -n 10`, `env VAR=1`) разбор уже видит насквозь.

**Пара тестов.** Отказ: `reboot`, `sudo shutdown -r +1`, `timeout 5 poweroff`, `/sbin/halt`.
Молчание: `test -f /var/run/reboot-required`, `grep reboot /var/log/syslog`,
`systemctl status poweroff.target`. С маркером: `reboot # CONFIRMED-DESTRUCTIVE` проходит.

---

### 2. Docker перестаёт молча уносить тома

**Что не так.** Три команды уносят данные, и ни одна не говорит ни слова. Худшая из них —
`compose down -v`: слово `down` читается как «останови», а `-v` уносит именованные тома,
то есть базу.

**Точка правки.** Новый модуль, ветка `docker` / `docker-compose`.

**Что должно стать.** Отказ на:

| Форма | Что уносит |
|---|---|
| `docker compose down -v` / `--volumes` (и `docker-compose down -v`) | тома проекта |
| `docker volume rm …` | названный том |
| `docker volume prune` | все неиспользуемые тома |
| `docker system prune -a` | образы, сети, кэш сборки |
| `docker system prune --volumes` | то же плюс тома |

**Ловушка.** `docker compose down` без флага безопасен — контейнеры пересоздаются, тома
остаются. Отказ на нём остановил бы обычный перезапуск.

**Вторая ловушка — разбор по позициям аргументов.** Существующая проверка написана как
`args[0] === 'system' && args[1] === 'prune'` (`exec-tool.ts:70`) и ломается на
`docker -H unix://… compose down -v` и `docker compose -f prod.yml down -v`: флаги сдвигают
позиции. Подкоманды ищутся последовательностью значимых слов с пропуском флагов. `docker
compose` и `docker-compose` приводятся к одной форме.

**Пара тестов.** Отказ на каждой форме выше. Молчание: `docker compose down`,
`docker compose up -d`, `docker image prune -a`, `docker volume ls`, `docker ps -a`,
`docker rm -f один-контейнер`.

---

### 3. Снос базы целиком требует подтверждения

**Что не так.** `dropdb production` не даёт ни предупреждения, ни отказа. `DROP DATABASE`
через клиента только предупреждает.

**Точка правки.** Новый модуль: имена команд и шаблон запроса.

**Что должно стать.** Отказ на: `dropdb`, `mysqladmin … drop`, `DROP DATABASE` в тексте
команды при вызванном клиенте БД, `redis-cli FLUSHALL` и `FLUSHDB`.

**Ловушка.** Команда клиента стоит не первым аргументом: `redis-cli -h db -p 6379 FLUSHALL`.
Искать среди всех аргументов, а не в `args[0]`.

**Честная граница, которую надо записать в README.** Запрос из файла не виден:
`psql -f drop.sql` и `psql < dump.sql` проедут молча — в команде текста запроса нет.
Проверка ловит форму, написанную руками, и это её потолок. Обещать больше нельзя.

**Пара тестов.** Отказ: `dropdb app`, `psql -c "DROP DATABASE app;"`, `redis-cli FLUSHALL`.
Молчание: `psql -c "DROP TABLE sessions;"` (предупреждение остаётся, но не отказ),
`createdb app`, `psql -l`, `psql -f migrations/003.sql`.

---

### 4. Задания cron не сносятся одной буквой

**Что не так.** `crontab -r` удаляет все задания пользователя разом, ничего не
переспрашивая. Клавиша соседняя с `-e`.

**Точка правки.** Новый модуль.

**Пара тестов.** Отказ: `crontab -r`, `sudo crontab -r -u deploy`. Молчание: `crontab -l`,
`crontab -e`, `crontab /etc/cron.d/new`.

---

### 5. Диски и тома

**Что не так.** `mkfs.ext4 /dev/sdz` молчит. Так же молчат `wipefs`, `dd` на устройство и
менеджеры томов.

**Точка правки.** Новый модуль, разбор аргументов.

**Что должно стать.** Отказ на: `mkfs.*`, `wipefs -a`, `dd` с `of=` на устройство,
`lvremove`, `vgremove`, `pvremove`, `zfs destroy`, `btrfs subvolume delete`.

**Ловушка, из-за которой разбор идёт по аргументам, а не по имени.** `dd` опасен не сам по
себе:

```
dd if=/dev/zero of=/swapfile bs=1M count=2048   ← штатное создание подкачки
dd if=/dev/zero of=/dev/sda                     ← сносит диск
dd if=/dev/sda of=/backup/disk.img              ← чтение диска, безопасно
dd if=/dev/zero of=/dev/null                    ← безвредно
```

Опасен только `of=`, указывающий на устройство, и `/dev/null` с `/dev/zero` из этого
правила исключаются. `wipefs` без `-a` читает подписи и ничего не портит.

**Раскрытие сервером — отказ.** `dd of="$DISK"`: куда укажет переменная, из текста не
видно. Решение то же, что у сторожа удаления (`destructive-command.ts:175-183`) — это не
«безопасно», а «проверить нечем», и такой случай останавливается.

**Пара тестов на каждую форму.** Отказ: четыре команды выше в опасных формах,
`lvremove /dev/vg0/data`, `zfs destroy tank/db`. Молчание: безопасные формы `dd` из
таблицы, `wipefs /dev/sda`, `lvs`, `vgs`, `zfs list`, `btrfs subvolume list /`.

---

### 6. Чтение после уничтожения — проверка 2 порога

Делается **последней**, после того как задачи 1–5 закрыты и проверены.

**Что не так.** Правильный порядок: копия → перенос → удаление. Ошибочный и частый:
удалили, потом обращаемся к тому, чего уже нет.

```
cp A A.bak && mv A B && rm -rf A     ← уничтожение последнее, молчание
rm -rf A && cp A A.bak && mv A B     ← чтение после уничтожения, отказ
dropdb app && pg_dump app > a.sql    ← тот же случай, объект не путь
```

Последняя строка — проверка того, что правило написано про объекты, а не про `rm`.
`dropdb`, `docker volume rm`, `mkfs` попадают под него без отдельной ветки.

**Точка правки.** Новый модуль. Разбор ролей — одна позиция, не грамматика команды.

**Как работает.**

1. Собрать объекты, которых команда лишает существования. Список уничтожителей — общий с
   задачами 1–5, нового не заводим: `rm -rf`, `mv` (по источнику), `dropdb`,
   `docker volume rm`, `mkfs`, `wipefs`.
2. Идти дальше по команде и искать эти объекты.
3. Нашли в позиции приёмника — молчим: объект создаётся заново.
4. Нашли в любой другой позиции — отказ.
5. Совпадение считается вниз по вложенности: удалили `A`, встретили `A/data` — тот же
   случай. Вверх не считается: удалили `A/build`, встретили `A` — не беда.

**Почему таблицы ролей на шесть команд здесь нет.** Она описывает каждую утилиту целиком —
что у неё источник, что приёмник, сколько их, — и на утилите вне таблицы не работает
вовсе. Разбор одной позиции работает на любой команде и умещается в три списка:

| Список | Содержимое | Зачем |
|---|---|---|
| Уничтожители | `rm -rf`, `mv`, `dropdb`, `docker volume rm`, `mkfs`, `wipefs` | общий с задачами 1–5 |
| Приёмник не последний | `-f` у `tar`/`zip`, `of=` у `dd`, `-t`/`--target-dir` у `cp`/`mv`/`rsync` | иначе беда проедет молча |
| Не считается чтением | `ls`, `test`, `stat` | иначе крик на проверке результата |

Приёмник по умолчанию — последний аргумент. Второй список — все известные исключения из
этого; без них `rm -rf A && cp -t /backup A` сочтёт `A` приёмником и пропустит потерю.
Третий нужен из-за штатной формы `rm -rf A && ls A` — проверки, что удаление прошло.

**Чего эта задача не поймает, и это записывается прямо в README.** Если удаление пришло
одним вызовом, а чтение — другим (агент подумал между ними), связка не видна: данные уже
потеряны к моменту второго вызова. Памяти между вызовами не заводим — состояние разошлось
бы с сервером, где путь могли пересоздать мимо нас.

Утилита со своей грамматикой, где приёмник стоит посередине и не помечен флагом, не
разбирается ничем — ни этим правилом, ни таблицей ролей.

**Что в этом плане поправил замер.** Два места, где правило об одной позиции ложно
кричало на штатной работе, — оба вскрылись живьём, а не юнитами:

- у `zip` архив стоит **первым** аргументом, а ключа под файл нет вовсе (`-f` значит
  «освежить»). По правилу «приёмник последний» роли переворачивались, и пересоздание
  архива `rm -rf arch.zip && zip -r arch.zip src` отказывалось;
- у `mkdir -p A B` приёмников **несколько**, и первый читался как источник: уборка
  `rm -rf A B; mkdir -p A B` отказывалась. Создание пустого (`mkdir`, `touch`, `mkfifo`)
  переведено в список «чтением не считается» рядом с осмотром;
- `mv -t DEST SRC` называет приёмник флагом, а список уничтоженного брал «все кроме
  последнего» — настоящая потеря проезжала молча, а существующий `DEST` объявлялся
  уничтоженным. Перенос ищет приёмник там же, где все остальные.

**Пара тестов.** Отказ: `rm -rf A && cp -r A B`, `rm -rf A; tar czf arch.tgz A`,
`rm -rf A && rsync -a A/ B/`, вложенность `rm -rf A && cp -r A/data B`,
`dropdb app && pg_dump app > a.sql`, исключение `rm -rf A && cp -t /backup A`.
Молчание: `rm -rf DEST && cp -r SRC DEST`, `cp -r A B && rm -rf A` (правильный порядок),
`rm -rf A && cp -r C D` (разные пути), `rm -rf A && mkdir -p A`,
`rm -rf A && git clone URL A`, `rm -rf A && ls A` (проверка результата),
`rm -rf A/build && tar czf arch.tgz A` (вложенность вверх).

---

## Чем закончится

- **README** — раздел про безопасность переписывается под две ступени: что предупреждает,
  что отказывает, как выглядит маркер. Обе честные границы (запрос из файла; удаление и
  копирование разными вызовами) записываются там же, а не остаются в спринте.
- **CHANGELOG** — это **breaking**: автоматизация, зовущая `reboot` или
  `docker compose down -v` через MCP, потребует маркера. Пишется до релиза v2.0.0.
  Отдельной строкой: перезагрузка роутера теперь требует **двух** маркеров в одной команде —
  `# CONFIRMED-REBOOT` для хука `router-no-reboot.sh` и `# CONFIRMED-DESTRUCTIVE` для нас.
  Маркеры принадлежат разным системам, объединять их нельзя.
- **ROADMAP** — строка спринта.
- `docs/tech-debt/` — всё найденное и не почин'енное сразу.

## Замечено рядом, в этот спринт не входит

- **Охраняемые пути в профиле.** Бэкапы разбором строки от мусора не отличить:
  `rm -rf /var/backups/db` — обычный путь, и сторож правильно его пропускает. Честное
  решение — список охраняемых каталогов в профиле, отказ по совпадению. Требует нового
  поля профиля, то есть сквозного теста «файл → загрузчик → конфигурация → инструмент»
  (правило №1 проекта), и поэтому идёт отдельной работой.
- ~~**`sudo -u deploy reboot` не распознаётся**~~ — **закрылось по дороге**, замер на обоих
  контейнерах 2026-08-16. Граница держалась на том, что значение флага обёртки неотличимо
  от имени команды; карта «обёртка → её флаги» из задачи 3 это различие вернула. Проверено
  живьём под пользователем `deploy`:

  ```
  sudo -u deploy reboot            ⛔ BLOCKED: "reboot" stops the machine
  sudo --user deploy shutdown -h now  ⛔ BLOCKED: "shutdown" stops the machine
  sudo -u deploy id -un            deploy      ← безобидная форма проходит
  ```

  Записано как факт, а не как рассуждение «форма бессмысленная, нужен root»: рассуждение
  было верным по последствиям, но неверным по поведению сторожа.

---

## Как стартовать в чистом контексте

Промпт агенту, который будет это делать. Всё, чего в нём нет, агент достроит сам — поэтому
факты названы явно.

```
Работаем в /Users/hypnosis/projects/ssh-mcp-server/.claude/worktrees/core11,
ветка feat/openssh-transport.

Спринт: docs/sprints/planned/FIX_17_refusal_where_loss_is_final.md — прочитай его
целиком и правила проекта в CLAUDE.md. Порядок работы: ASK → PLAN → CODE, задачи
идут по одной, после каждой пауза и отчёт диффом.

Разведка кода — только через семантический поиск (mcp__lumen__semantic_search),
не грепом по дереву. grep — для точного известного имени. Правило действует и
для тебя, и для субагентов, которых позовёшь: в их промпт клади его дословно.

Состояние на старте:
- Спринт FIX_16 закрыт и проверен, его изменения лежат в рабочем дереве
  НЕЗАКОММИЧЕННЫМИ. Не откатывай их и не трогай, пока владелец не скажет.
- Разбор команды на вызовы уже написан: src/utils/command-parse.ts,
  функция parseInvocations даёт имя команды и аргументы по каждому сегменту.
  Второй разбор не заводи.
- Отказ и маркер готовы: destructive-command.ts, CONFIRMATION_MARKER и
  blockedMessage. Маркер один на всё, второго не вводим.
- Точка, куда встраивается новый разбор: exec-tool.ts:401 refuseDestructive,
  он вызывается из handleCall до первой отправки (exec-tool.ts:213).

Чем проверять:
  npx tsc --noEmit
  npx vitest run tests/unit/
  npm run mutate -- HEAD          # по незакоммиченному, перед каждым отчётом
  npm run lab:up                  # лаборатория: alpine 2231, debian 2232, ключ .lab/key
  SSH_MCP_LIVE=1 npx vitest run tests/live/

Живые замеры делай своей оснасткой: подключённый MCP-сервер крутит dist/ со старта
сессии и твоих правок не видит. Скрипт-обёртка поднимает свежий dist через Client из
SDK, держи его в scratchpad, в репозиторий не клади. Профили: лаборатория —
~/.mcp-config/acceptance-profiles.json, боевые (включая finlab) —
~/.mcp-config/docker-profiles.json.

Отказ выносится ДО отправки, поэтому проверяется на любом контейнере: docker, lvm и
psql на нём не нужны. На finlab — только чтение и только проверка, что штатные
команды не задеты.

Требования к отчёту каждой задачи:
- дифф и дословный вывод команд, не слово «сделано»;
- пара тестов: отказ на опасной форме И молчание на соседней штатной;
- отдельный замер, что команда с # CONFIRMED-DESTRUCTIVE проходит;
- разбор выживших мутантов: какие настоящие дыры, какие эквивалентные и почему.

Ответ «здесь менять нечего, вот замер» — нормальный и ожидаемый. Не воспроизвелось —
не выдумывай правку. Вывод команд руками не сочиняй: правило №6 проекта.
```
