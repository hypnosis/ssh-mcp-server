# planned/

Запланированные спринты, ещё не перенесённые в работу/архив.
Завершён → перенести в `../archive/`. Отменён → завести `../deprecated/`.

Формат имени: `CATEGORY_NN_short-name.md` (как в `../archive/`).
