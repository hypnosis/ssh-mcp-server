# TD-01. Код, который не заметит поломки

**Дата:** 2026-08-04
**Статус:** 🔴 открыт, не чиним
**Откуда:** первый прогон мутационного тестирования (`npm run mutate`), спринт CORE_10

---

## Что случилось

Поставили Stryker и прогнали его по файлам, которые тронул спринт. Он ломает код
сотнями мелких способов и смотрит, заметит ли хоть один тест. Не заметил —
значит на этом месте тестов нет, сколько бы строк ни было «покрыто».

Ниже — то, что он нашёл. **Ничего из этого не чиним сейчас**: это список, а не
задача. Чинить — отдельным решением, когда дойдут руки до конкретного файла.

## Как читать цифры (иначе они врут)

Мутации гоняют **только юниты**. Живые тесты в прогон не входят: им нужна
поднятая лаборатория, и каждый мутант означал бы отдельный прогон по SSH —
минуты превратились бы в часы.

Отсюда перекос: `file-tools.ts` и `snapshot-tool.ts` проверяются в основном
живыми тестами, поэтому их балл занижен и катастрофы не означает. А вот `utils`
живыми тестами почти не покрыты — там цифра честная.

## Находки

| Файл | Балл | Выжило | Без покрытия | Что это значит |
|------|------|--------|--------------|----------------|
| `src/managers/ssh-manager.ts` | 0% | 0 | 119 | юнитов нет вообще |
| `src/tools/snapshot-tool.ts` | 16% | 134 | 137 | самое слабое место из живого кода |
| `src/tools/file-tools.ts` | 36% | 102 | 176 | закрыт в CORE_11, задача 4.12 |
| `src/utils/logger.ts` | 36% | 38 | 14 | проверена только маскировка секретов |

### Точечно

**`src/utils/sha256.ts:77` — `localFileSize` не вызывает ни один тест.**
Экспортируемая функция, `[NoCoverage]`. Найдена мутацией за десять секунд;
раньше такое искали грепом руками. Оказалась мёртвой целиком — не только тесты,
её не звал никто, — и удалена в CORE_11, задача 3.4. Прогон `knip` по тому же
следу нашёл ещё пятнадцать имён: [TD-11](unused-exports_11.md).

**`src/utils/logger.ts` — методы `debug` и `info` можно опустошить целиком**,
и все 631 тест останутся зелёными. То же с текстом уровня: `this.log('debug', …)`
меняется на `this.log('', …)` незамеченным. Логгер проверяется только со стороны
секретов, само логирование — нет.

**`src/managers/ssh-manager.ts` — 119 мутантов, ни одного убитого.**
Отпало вместе с бэкендом `ssh2`: файл удалён в CORE_11, задача 2.1.

**Слой инструментов чинится по одному файлу в CORE_11, чанк 4.** `exec-tool.ts` закрыт
задачей 4.9 (133 выживших → 31): проверяются объявление инструмента и форма ответа —
то, что читает агент, а не только факт отправки команды. Остальные четыре —
`audit-tool`, `transfer-tool`, `file-tools`, `log-tools` — закрыты задачей 4.12.

`audit-tool.ts` закрыт задачей 4.12 (403 → 150 выживших, 29.61 → 75.13): проверяются разбор
ответа сервера, сборка команды и печать сводки — последняя сверкой текста целиком, потому что
по кускам отступы и ширину колонок не проверяет никто. Там же исправлено то, что мутанты
показали в самих тестах: мок отвечал заготовкой, не глядя на команду, и потерянный раздел
остался бы незамеченным.

`transfer-tool.ts` закрыт задачей 4.12 (158 → 19 выживших, 67.15 → 95.69, не покрыто 131 → 2):
запись под root собирается по частям, вид пути решается по двум буквам ответа `test`, а текст
ответа — единственное, что видит агент. Мок здесь ведёт своё дерево путей и отвечает по
состоянию, а неизвестную команду отклоняет: заготовка на любой запрос скрыла бы и потерянный
кусок команды, и обращение не к тому пути.

`file-tools.ts` закрыт задачей 4.12 (110 → 28 выживших, 35.96 → 93.48, не покрыто 175 → 1):
сводка по пачке файлов сверяется целиком — по кускам ни отступ, ни потерянный разделитель никто
не заметит. Там же разведены три исхода чтения, которые раньше сливались: содержимое, отказ
сервера и обрезанный ответ. Объявление инструмента проверяется формой, а не текстом: у каждого
поля обязан быть тип, у объявленного описания — непустое значение.

`log-tools.ts` закрыт задачей 4.12 (96 → 7 выживших, 37.50 → 97.70, не покрыто 94 → 0): сервер
в тестах сам исполняет `tail` и `grep` по своим журналам, поэтому потерянный флаг виден. До
этого у файла не было юнитов вовсе — задача 4.11 дала ему только живого сторожа на строку
поиска, а балл не двигался, потому что мутации гоняют юниты.

**`runner/process.ts` закрыт задачей 4.10** (31 → 22): уборка за завершившейся командой
снаружи не видна ни по какому полю результата, поэтому проверяется другими глазами —
счётчиком таймеров, удерживающих цикл событий, и списком слушателей на сигнале отмены.
Там же нашлось настоящее: запись в закрытый командой ввод без своего обработчика уносит
весь процесс сервера.

## Замер на границе чанка 4 (2026-08-08)

Прогон по всем 44 файлам, которые ветка `feat/openssh-transport` изменила относительно `main`.
Слой инструментов после задач 4.9 и 4.12 закрыт, а картина по остальному такая.

| Файл | Балл | Выжило | Без покрытия | Решение |
|------|------|--------|--------------|---------|
| `src/tools/snapshot-tool.ts` | 16% | 134 | 137 | не чиним: формат ответа спринт не трогал |
| `src/tools/monitoring-tool.ts` | 49% | 44 | 64 | то же |
| `src/utils/ssh-config.ts` | 3% | 2 | 72 | чтение профилей — см. ниже |
| `src/utils/profile-resolver.ts` | 16% | 89 | 105 | то же |
| `src/utils/profiles-file.ts` | 40% | 121 | 79 | то же |
| `src/utils/logger.ts` | 36% | 38 | 14 | было и раньше, см. выше |

**Чтение профилей — самое слабое место вне инструментов.** Файл профилей мы принимаем на веру
больше всего: из него берутся адрес, пользователь, ключ и правила путей. Проверен он меньше
всего — три файла подряд с баллом ниже сорока. Отдельная задача, не в этот спринт.

**Свежий код транспорта выглядит хуже, чем есть.** `openssh-runner` 62%, `runtime-check` 54%,
`askpass` 61%, `errors` 48% — все они проверяются живой сеткой на трёх контейнерах, а мутации
гоняют одни юниты. Цифра здесь занижена по построению, как и было сказано выше про `file-tools`
до задачи 4.12.

## Чего здесь делать не надо

Не гнаться за мутационным баллом. Он не цель и для половины файлов вообще не
показателен. Работающий способ пользоваться инструментом описан в `CLAUDE.md`:
смотреть на **выживших мутантов в том, что только что написал**, а не на процент
по проекту.
