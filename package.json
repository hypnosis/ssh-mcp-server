{
  "name": "@hypnosis/ssh-mcp-server",
  "version": "2.0.1",
  "description": "Run commands, move files, tail logs and audit live servers over SSH from an AI coding agent — Claude Code, Codex, Gemini CLI, Hermes, or any MCP client. Uses the system OpenSSH client, keys and config.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "ssh-mcp-server": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "secrets.json.example"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit/",
    "test:live": "SSH_MCP_LIVE=1 vitest run tests/live/",
    "lab:up": "bash scripts/lab-up.sh",
    "test:watch": "vitest",
    "mutate": "node scripts/mutate-changed.mjs",
    "prepublishOnly": "npm run build && npm run test:unit",
    "publish:dry": "npm pack --dry-run",
    "publish:check": "npm pack && tar -tzf *.tgz | head -20 && rm -f *.tgz",
    "publish:npm": "npm publish --access public"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "@types/node": "^20.11.0",
    "tsx": "^4.23.12",
    "typescript": "^5.3.0",
    "vitest": "^4.1.10"
  },
  "overrides": {
    "qs": "^6.15.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "mcp-server",
    "ssh",
    "openssh",
    "scp",
    "sftp",
    "remote-server",
    "server-management",
    "devops",
    "sysadmin",
    "logs",
    "audit",
    "claude",
    "claude-code",
    "codex",
    "gemini-cli",
    "hermes",
    "ai-agent"
  ],
  "author": "hypnosis",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hypnosis/ssh-mcp-server.git"
  },
  "homepage": "https://github.com/hypnosis/ssh-mcp-server#readme",
  "bugs": {
    "url": "https://github.com/hypnosis/ssh-mcp-server/issues"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  }
}
