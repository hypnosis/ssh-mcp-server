import { defineConfig } from 'vitest/config';

/**
 * Конфиг только для мутационных прогонов.
 *
 * Основной `vitest.config.ts` объявляет два проекта, и живой требует поднятой
 * лаборатории. Stryker перед работой делает пробный прогон и, если тот красный,
 * отказывается запускаться вовсе — то есть без этого файла мутации не поедут
 * ни разу, пока не подняты контейнеры.
 *
 * Мутации спрашивают «заметит ли тест поломку кода», и отвечают на это юниты.
 */
export default defineConfig({
  test: {
    include: ['tests/unit/**/*.test.ts'],
  },
});
